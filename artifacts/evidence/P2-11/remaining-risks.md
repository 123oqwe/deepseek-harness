## Remaining Risks (P2-11)
1. Hot-swap during in-flight actions — needs integration with Action Manifest (P2-03).
2. UI/headless dump output — needs integration with CLI dump-config.
3. Capability diff between profiles — interface defined, integration deferred.
4. Custom profile creation UI — deferred to operator control plane (P8-08).
