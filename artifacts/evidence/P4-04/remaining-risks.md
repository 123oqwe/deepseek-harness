## Remaining Risks (P4-04)
1. Concurrent amendment CAS (compare-and-swap) — needs integration with durable store.
2. Real kernel signing — currently uses SHA-256; needs integration with Trust Kernel (P0-02).
3. Replay-based revision timeline reconstruction — needs integration with audit ledger.
4. Plan amendment UI — deferred to operator control plane (P8-08).
