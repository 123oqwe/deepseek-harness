GOAL: 按 SONNET-EXECUTOR-START-PROMPT.txt 执行全部 109 项（100 registry + P9-01…09 扩展）：W0 六件套全绿 → W1–W19 → R10 → W20–W22；终态 = 100/100 ACCEPTED + R10 通过 + P9 九项全部 VERIFIED 或 scheduled-BLOCKED 在案。
NOW（2026-09-01）: W0 六件套已全绿。**P0-01 已 ACCEPTED —— First-100 首个 ACCEPTED 行，1/109。** BLOCKED-001~005 均已 ANSWERED 并落地：generate-ledger.mjs 的 --accept/--supplement + 三谓词（commit 181b048072/0b25d48068）、P0-01 F-stage supplement（灵敏度证明+独立 Reviewer ACCEPT+双重突变验证+真实签名 CI，run 33473101218，0 失败）、--supplement 记录观察 + --accept 转正（commit 6dcbf91e6e，independentVerdict=APPROVED/status=ACCEPTED）。过程中拿自己刚写的三谓词对 P0-01 真数据 dogfooding，抓到两个真 bug 并修复：谓词(i) OR/AND 语义（已修复为 AND）、generate-ledger.spec.ts 依赖真实仓库 git 历史在 CI 浅克隆下真实报错（已用 throwaway git fixture 重写，非 flake）。BLOCKED-006/007（gq-92 裁决，通知协议+吞吐优化）已记录折入 RULES。**W1 已完整关账**：row ACCEPTED✓ + composition smoke✓（test:snapshot 确认间歇性 flaky 但非回归，clean run 110/112 通过）+ 全量CI✓（run 33475234136，对 HEAD 6dcbf91e6e，attest.ts 独立验签通过，17451/17519 通过 0 失败）+ ledger 已 --init 重生成✓。**W2 正式开闸**（P0-02 确立 Trust Kernel 边界、P0-06 建立 Schema Registry，均仅依赖 P0-01）——尚未开始任何 RED/GREEN，下一步是为 W2 的第一个 epic 走 SPEC-FREEZE。C7：guanjieqiao-92 为常设 maintainer 技术裁决代理（范围见 decisions-approved.md 追加批复 C7，钱/凭证/不可逆/master/decisions-approved.md 既批条目/109 范围/五条铁律仍须 park 本人）。
RULES: 每次 compact/新会话先重读本文件 → spec/first100/exec/EXEC-STATE.json → decisions-approved.md → BLOCKED-QUEUE.md → ledger 当前行 → registry 当前项，会话记忆一律不作数；不确定即 BLOCKED，禁猜；禁跳 wave；写码全在 first100-exec 分支；hook 未注册 ⇒ BLOCKED。子代理（Writer/Reviewer）禁止在共享 worktree 执行 git checkout/switch/reset 等分支或树状态变更（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：需要在 attempt 分支写码的子代理必须用 isolation:"worktree" 派发，或由 Supervisor 先切好目标分支、子代理全程不碰 git 分支命令；每次 dispatch 前后校验 `git branch --show-current`/HEAD 未被子代理变更，变了即整批产出作废、树恢复后重派，不做部分打捞。BLOCKED-006（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：新 BLOCKED-00N 条目创建即 SendMessage 全文推 guanjieqiao-92 + osascript 通知用户，不分 C7①②范围；等待裁决期 ScheduleWakeup ≤15min；推送失败或 60 分钟未裁决 ⇒ osascript 通知用户本人+维持 park，不自行裁决；仅约束今后新条目。BLOCKED-007（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：①当前 wave ≥2 epic 且 files[] 互斥（owner map 已核 0 冲突）即可开 2-3 条并行 Writer lane（每 lane 必须 isolation:"worktree"，质量门不打折）；②CI 观察在途时下一 slice 的 SPEC-FREEZE/RED/GREEN 照常推进，禁止空等，唯一串行点是 wave 关账与行级三谓词校验；③flake 登记册语义（失败集合⊆登记册即该 run 观察对其余测试有效，免重跑）——登记册文件本身待建，新条目仍属 C7 flake 定性范围推裁决，建成前任何失败仍须 clean rerun。
