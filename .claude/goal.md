GOAL: 按 SONNET-EXECUTOR-START-PROMPT.txt 执行全部 109 项（100 registry + P9-01…09 扩展）：W0 六件套全绿 → W1–W19 → R10 → W20–W22；终态 = 100/100 ACCEPTED + R10 通过 + P9 九项全部 VERIFIED 或 scheduled-BLOCKED 在案。
NOW（2026-09-01）: W0 六件套已全绿。**P0-01、P0-06 均已 ACCEPTED（2/109）。W1 已完整关账。W2 进行中**：P0-06 的真实签名 CI 观察为 run 33536357047 @5805b22e21（0/17622 失败，登记 flake 未复发），三谓词全过；P0-06.F 收尾过程中另修复三处缺陷：BLOCKED-012 多 patch 机制回填至 first100-exec 主线（此前只存在于 attempt 分支）、P0-06.C/P 引入的 11 处 lint 违规（exact-sha workflow 从不跑 lint 导致隐形）、P0-01.U 遗留的 verify-cordis-config 依赖声明缺口。**P0-02（Trust Kernel）F 格：用户已就 BLOCKED-011 作出最终裁决（"行"，经 guanjieqiao-92 转达）——P0-02.F 现在以当前诚实记录状态收口，不再等待/不再开第 5 轮**。裁决内容：①`verify-trust-kernel-property-access` 门维持现状（3 分支纯词法匹配 + 显式 allowlist，commit 序列见 BLOCKED-QUEUE.md）；②新增常设硬触发规则——今后任何 epic 接线真实 policy/audit/signature-verifier 强制点前（预计 ~W6），vendored Cordis Fiber 结构修复（选项 A）为硬前置条件，该触发 epic 的 Reviewer 必须在 --accept 前显式核验选项 A 已落地；③6 处 known-limitation 文档面（EN/ZH boundary doc、EN/ZH package README、src/index.ts JSDoc、AGENTS.md）已加注"deferred, not permanently accepted"+选项 A 触发条件并提交（commit f55b54b2ae，doc-budget ceiling 2010→2030 已同步调整并通过 verify-doc-budgets）；④`.claude/goal.md` 本次更新即落实此裁决。**下一步（P0-02.F 收口剩余步骤）**：确认/重冻 P0-02.F 真实 command-freeze 条目（覆盖 dispose/pin-hardening/boot/boundary/trust-kernel-posture 等最终测试面，e7d49cfa6b 从未真正冻结过 F 格条目）→ 合并 first100-exec 至 attempt/P0-02-F（拾取 docs/testing.md 既有修复、SERVICE_WALK_EXEMPTIONS trustKernel 豁免等）→ 反向合并回 first100-exec → 为 P0-02 建 acceptance-coverage.json → 真实 CI push（经 guanjieqiao-92）→ 绿 F 格账 → --accept（三谓词）→ 回报 guanjieqiao-92 独立复核（3/109）→ W2 关账协议（composition smoke + 全量 CI + ledger 重生成 + 继任 supervisor 关卡）→ W3 开闸。BLOCKED-001~013 全部 ANSWERED（BLOCKED-011 现为 ANSWERED-BY-USER，终态）。C7：guanjieqiao-92 为常设 maintainer 技术裁决代理（范围见 decisions-approved.md 追加批复 C7，钱/凭证/不可逆/master/decisions-approved.md 既批条目/109 范围/五条铁律仍须 park 本人；远端 push/CI 触发现由 guanjieqiao-92 代执行，用户已授权）。
RULES: 每次 compact/新会话先重读本文件 → spec/first100/exec/EXEC-STATE.json → decisions-approved.md → BLOCKED-QUEUE.md → ledger 当前行 → registry 当前项，会话记忆一律不作数；不确定即 BLOCKED，禁猜；禁跳 wave；写码全在 first100-exec 分支；hook 未注册 ⇒ BLOCKED。子代理（Writer/Reviewer）禁止在共享 worktree 执行 git checkout/switch/reset 等分支或树状态变更（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：需要在 attempt 分支写码的子代理必须用 isolation:"worktree" 派发，或由 Supervisor 先切好目标分支、子代理全程不碰 git 分支命令；每次 dispatch 前后校验 `git branch --show-current`/HEAD 未被子代理变更，变了即整批产出作废、树恢复后重派，不做部分打捞。BLOCKED-006（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：新 BLOCKED-00N 条目创建即 SendMessage 全文推 guanjieqiao-92 + osascript 通知用户，不分 C7①②范围；等待裁决期 ScheduleWakeup ≤15min；推送失败或 60 分钟未裁决 ⇒ osascript 通知用户本人+维持 park，不自行裁决；仅约束今后新条目。BLOCKED-007（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：①当前 wave ≥2 epic 且 files[] 互斥（owner map 已核 0 冲突）即可开 2-3 条并行 Writer lane（每 lane 必须 isolation:"worktree"，质量门不打折）；②CI 观察在途时下一 slice 的 SPEC-FREEZE/RED/GREEN 照常推进，禁止空等，唯一串行点是 wave 关账与行级三谓词校验；③flake 登记册语义（失败集合⊆登记册即该 run 观察对其余测试有效，免重跑）——登记册文件本身待建，新条目仍属 C7 flake 定性范围推裁决，建成前任何失败仍须 clean rerun。BLOCKED-010（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，自 P0-02.F 起生效）：每个 Reviewer 派发在给 verdict 前必须对本 slice 触及表面运行相关仓库级 gate 子集（至少考虑 docs/module-graph 一致性、verify-export-jsdoc、check-workspace-constraints、双语配对，按改动面选最小相关子集）；发现违规=本 slice 的 MUST-FIX，禁止遗赠给下一 stage，清偿边界是 slice 不是 epic。BLOCKED-011 点4（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，跨 epic 前向约束）：今后任何接线真实 policy/audit/signature-verifier provider 的 epic（P2-05 等），其消费方必须经 `ctx.get('trustKernel')` 读取内核（禁止裸 `ctx.trustKernel` 属性访问，受 P0-02.F 新增机械门 `scripts/verify-trust-kernel-property-access.ts` 覆盖）；该 epic 的 Reviewer 必须显式核验此项，不修改任何 registry acceptance 文本，仅作审查清单强制项。BLOCKED-012（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，全 epic 适用）：`files[]` 偏离 wave-map 字面声明仅当四条件全满足才合法——①强制性（真实技术约束逼出+证据，Reviewer 独立确认字面写法会真失败，非图方便）②无 owner 冲突（查 spec/first100-owner-map.json，落他 epic 的 [N]/[P] owned 则 CONFLICT→park）③记 manifest-patch（tests/first100/adjudication.json#deliverablePathPatches，支持一 epic 多条 patch 经可选 `epic` 字段区分 key，不改 registry/matrix 源条款文本）④冻结测试仍须落在（原或替代）files[] 内，B4(e)/B7① 不因偏离松动；任一不满足→MUST-FIX。BLOCKED-013（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，扩展 BLOCKED-010 仓库门子集）：任何触及 Cordis Context/Events merge 声明、或触及 typert 生成型 catalog 扫描面（wire/schema/settings-registration 类改动最常见）的 slice，Reviewer 除 BLOCKED-010 原有子集外，还必须额外运行 `pnpm run verify-cordis-catalog`/`verify-cordis-api`/`verify-cordis-inspect-catalog`（按适用性选择）；发现违规同样是本 slice 的 MUST-FIX。（起因：P0-06.U 合并后真实 CI 失败，cordis-catalog 字节复现测试因该门未列入 P0-06.U 复审子集而漏检。）**新增标准安排（2026-09-01，用户已授权 guanjieqiao-92）**：本会话若被分类器拦截 `git push`/`gh workflow` 等远端可逆操作，直接把"要推的分支/要触发的 SHA"发给 guanjieqiao-92，由其执行并回传 run ID——不必反复重试或长时间卡等；真正不可逆操作（force push 覆盖历史、动上游 master、删除、对外发布）仍必须回用户本人，谁都不能代劳。first100-exec 权威状态：详见 NOW 行最新叙述（本文件在多个 worktree 间可能存在滞后副本，以 first100-exec 分支上的版本为准）。**待办（低优先，下个安全断点做，ANSWERED-BY-DELEGATE(gq-92) 2026-09-01）**：`.github/workflows/first100-exact-sha.yml` 从不跑 `pnpm run lint`，导致 P0-06.C/P 引入的 11 处 lint 违规隐形累积、全靠 Reviewer 人工抓（与 cordis-catalog 缺口同类）。应把 lint 纳入该 workflow 或每 slice 固定门项，防止此类 CI 覆盖缺口重复发生。
