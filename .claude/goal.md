GOAL: 按 SONNET-EXECUTOR-START-PROMPT.txt 执行全部 109 项（100 registry + P9-01…09 扩展）：W0 六件套全绿 → W1–W19 → R10 → W20–W22；终态 = 100/100 ACCEPTED + R10 通过 + P9 九项全部 VERIFIED 或 scheduled-BLOCKED 在案。
NOW（2026-09-01）: W0 六件套已全绿。**P0-01、P0-06、P0-02、P0-03、P0-05 均已 ACCEPTED（5/109，2026-09-02 三谓词经 delegate 独立字节级核验后授权 --accept，见 goal.md 末尾 NOW 行追加）。W1、W2 均已完整关账。W3 现已开闸**。P0-02 收口：真实签名 CI 观察 run 33548460030 @74a6304ae0（17637/17637 全过，0 失败，无需 flake 吸收）；三谓词 --accept 于 commit c6355f7eab 通过；guanjieqiao-92 独立复核三谓词确认无误（逐字核对 10 条 expectCases 与真实 CI 输出，含转义引号后语义一致）。收口过程修复：command-freeze 条目此前从未真正冻结过 F 格（commit a3f03cfba5 补冻，dispose.spec.ts+pin-hardening.spec.ts 10/10；BLOCKED-012 manifest-patch ×2，count 4→6）；4 条 expectCases 标题与真实 CI 输出逐字核对后发现并订正措辞漂移（commit 4daec92432）；P0-02 四条 acceptance-coverage.json 条目补齐。BLOCKED-011 以用户终裁收口：门 3 分支纯词法闭合+已知残留独立证明当前不可达+新增常设硬触发规则（今后任何 epic 接线真实 policy/audit/signature-verifier 强制点前，预计 ~W6/P2-05，vendored Cordis Fiber 结构修复即"选项 A"为硬前置）；6 处 known-limitation 文档面已诚实标注 deferred-not-accepted。**W2 关账协议四项证据（BLOCKED-004 point 5）全部满足**：①行级 ACCEPTED——wave=2 仅 P0-02/P0-06 两 epic，均 ACCEPTED（P0-02 P 格 registry 声明 N/A，非缺口）；②composition smoke——`pnpm run test:snapshot` 两次全量跑，均 109 passed/2 skipped/1 failed（同一 `replays session-title-after-turn through dsh --profile sdk` 用例），单独隔离重跑清洁通过，且该测试文件历史与本波改动（trust-kernel/schema-registry）零关联——确认为预存、非回归的负载态 flaky，判定基准与 W1 关账先例（commit 299474010d）一致；③全量 CI——run 33550498365 @2950bec7ec，17637/17637 全过、0 失败（真正干净，无需 flake 吸收）；④ledger 全量重生成——`generate-ledger.mjs --init` 前后 diff 除 lastUpdatedUtc/inputDigest 外零字节差异（比"仅本波格变化"更严格：零变化）。**继任 supervisor 冷启动关卡**：首次冷启动独立核验（零会话记忆，仅读 goal.md+EXEC-STATE.json+decisions-approved.md+BLOCKED-QUEUE.md+ledger.json/.md+registry.json）给出 NO-GO——正确指出彼时 composition-smoke/全量 CI 证据尚未写入任何持久化文件（这正是该关卡设计要抓的缺口，机制验证有效）；本次提交把全部证据落盘后，第二次冷启动核验预期读到 GO，证据见下一条提交。EXEC-STATE.json currentWave 2→3。**W3 进行中**：F6 确认 5 epic（P0-03/P0-05/P0-07/P1-01/P2-01）两两互斥、predecessors 全就绪，guanjieqiao-92 授权 3 lane 滚动（先开 P0-03/P0-05/P0-07）。三条首轮复审全 REJECT（由此新增 BLOCKED-014），修完二审/三审全部 ACCEPT：P0-03（4 处发现：C 格越界文件系统扫描+零 manifest-patch、acceptance[0] 漏测、consumers[] 遗漏——全修，二审 ACCEPT）、P0-07（纯机械 module-graph 漏 regen——修，二审 ACCEPT）、P0-05（must[1] 敏感参数保证从注释级加固为真实 branded RedactedJsonValue 类型+编译期拒绝测试，dsh-util-values 分类核实为已符合先例，module-graph regen——二审又抓到漏双语 README，三审 ACCEPT）。**三条均已合并 first100-exec + 冻结 command-freeze + 真实 CI 绿账**：P0-03.C/P0-07.C 用同一批 CI（run 33560054567 @d21e75068d，17694/17694 干净）分绑两份内容等价字节不同的 report（B7①互异性）；P0-05.C 待推送绿账（合并 commit 438683df38，冻结条目本轮已写）。绿账过程中抓到一次真实手误——candidate-sha 打串时后半段编造、格式合法但对象不存在，generate-ledger.mjs 只查正则不查 git object 真实性，靠 Supervisor 自己 git rev-parse 交叉核对才逮到、当场用真值覆盖修正——由此新增 BLOCKED-015（结构修法归入 BLOCKED-014 同批"下个安全断点"）。P0-05.C 已绿账（run 33562243795 @92174bb734，17726/17726 干净，commit 45160a02ae）。三 epic 均尚未 ACCEPTED（P/U/F 未跑）。批处理策略生效（guanjieqiao-92 裁定，2026-09-01）：不再每格单推——本地把下一阶段的 RED/GREEN/独立复审/合并 first100-exec 攒够（某 epic 四格齐可 --accept，或跨 lane 攒够 3-4 绿格，先到先推）再一次性 push+触发批 CI，省配额；每格 candidate-SHA 各自绑定 + B7①各用互异 observation。已派发下一阶段 Writer：P0-03→U、P0-05→P、P0-07→P，三条并行跑。P1-01/P2-01 待补入空出的 lane。**‼️程序级重大变更（BLOCKED-016，用户已定，2026-09-01）：基线重锚到上游 4e84901e 已确定，将在 W3 关账后、W4 开闸前作专门 BASE-ALIGN-v2 mini-wave 执行；硬约束——未完成 BASE-ALIGN-v2 不得开 W4，任何会话读到"W3 已关账"后必须先查 BLOCKED-016 全文再决定是否可进 W4。W3 当前照常在 0a53fb55 上不停不改。** 23 个 PARTIAL epic 的缩范围规格已 vendor 到 `spec/first100/sources/base-align-v2/23-partial-rescope-spec.md`（BASE-ALIGN-v2 时据此改 registry，不是现在）。**下一步**：三条 next-stage Writer 完成后二审→按批处理阈值攒够即推 CI→继续走各自剩余阶段直到齐格 --accept；同时留意 W3-close 触发点、届时执行 BLOCKED-016。BLOCKED-001~016 全部 ANSWERED（BLOCKED-011 为 ANSWERED-BY-USER，终态；BLOCKED-014/015 结构治本待办见 RULES；BLOCKED-016 为硬约束，见上）。C7：guanjieqiao-92 为常设 maintainer 技术裁决代理（范围见 decisions-approved.md 追加批复 C7，钱/凭证/不可逆/master/decisions-approved.md 既批条目/109 范围/五条铁律仍须 park 本人；远端 push/CI 触发现由 guanjieqiao-92 代执行，用户已授权）。
RULES: 每次 compact/新会话先重读本文件 → spec/first100/exec/EXEC-STATE.json → decisions-approved.md → BLOCKED-QUEUE.md → ledger 当前行 → registry 当前项，会话记忆一律不作数；不确定即 BLOCKED，禁猜；禁跳 wave；写码全在 first100-exec 分支；hook 未注册 ⇒ BLOCKED。子代理（Writer/Reviewer）禁止在共享 worktree 执行 git checkout/switch/reset 等分支或树状态变更（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：需要在 attempt 分支写码的子代理必须用 isolation:"worktree" 派发，或由 Supervisor 先切好目标分支、子代理全程不碰 git 分支命令；每次 dispatch 前后校验 `git branch --show-current`/HEAD 未被子代理变更，变了即整批产出作废、树恢复后重派，不做部分打捞。BLOCKED-006（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：新 BLOCKED-00N 条目创建即 SendMessage 全文推 guanjieqiao-92 + osascript 通知用户，不分 C7①②范围；等待裁决期 ScheduleWakeup ≤15min；推送失败或 60 分钟未裁决 ⇒ osascript 通知用户本人+维持 park，不自行裁决；仅约束今后新条目。BLOCKED-007（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：①当前 wave ≥2 epic 且 files[] 互斥（owner map 已核 0 冲突）即可开 2-3 条并行 Writer lane（每 lane 必须 isolation:"worktree"，质量门不打折）；②CI 观察在途时下一 slice 的 SPEC-FREEZE/RED/GREEN 照常推进，禁止空等，唯一串行点是 wave 关账与行级三谓词校验；③flake 登记册语义（失败集合⊆登记册即该 run 观察对其余测试有效，免重跑）——登记册文件本身待建，新条目仍属 C7 flake 定性范围推裁决，建成前任何失败仍须 clean rerun。BLOCKED-010（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，自 P0-02.F 起生效）：每个 Reviewer 派发在给 verdict 前必须对本 slice 触及表面运行相关仓库级 gate 子集（至少考虑 docs/module-graph 一致性、verify-export-jsdoc、check-workspace-constraints、双语配对，按改动面选最小相关子集）；发现违规=本 slice 的 MUST-FIX，禁止遗赠给下一 stage，清偿边界是 slice 不是 epic。BLOCKED-011 点4（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，跨 epic 前向约束）：今后任何接线真实 policy/audit/signature-verifier provider 的 epic（P2-05 等），其消费方必须经 `ctx.get('trustKernel')` 读取内核（禁止裸 `ctx.trustKernel` 属性访问，受 P0-02.F 新增机械门 `scripts/verify-trust-kernel-property-access.ts` 覆盖）；该 epic 的 Reviewer 必须显式核验此项，不修改任何 registry acceptance 文本，仅作审查清单强制项。BLOCKED-012（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，全 epic 适用）：`files[]` 偏离 wave-map 字面声明仅当四条件全满足才合法——①强制性（真实技术约束逼出+证据，Reviewer 独立确认字面写法会真失败，非图方便）②无 owner 冲突（查 spec/first100-owner-map.json，落他 epic 的 [N]/[P] owned 则 CONFLICT→park）③记 manifest-patch（tests/first100/adjudication.json#deliverablePathPatches，支持一 epic 多条 patch 经可选 `epic` 字段区分 key，不改 registry/matrix 源条款文本）④冻结测试仍须落在（原或替代）files[] 内，B4(e)/B7① 不因偏离松动；任一不满足→MUST-FIX。BLOCKED-013（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，扩展 BLOCKED-010 仓库门子集）：任何触及 Cordis Context/Events merge 声明、或触及 typert 生成型 catalog 扫描面（wire/schema/settings-registration 类改动最常见）的 slice，Reviewer 除 BLOCKED-010 原有子集外，还必须额外运行 `pnpm run verify-cordis-catalog`/`verify-cordis-api`/`verify-cordis-inspect-catalog`（按适用性选择）；发现违规同样是本 slice 的 MUST-FIX。（起因：P0-06.U 合并后真实 CI 失败，cordis-catalog 字节复现测试因该门未列入 P0-06.U 复审子集而漏检。）BLOCKED-014（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，取代 BLOCKED-010 原"按改动面选子集"表述，仅限下列 5 门）：每个 slice（Writer 交付前 + Reviewer 复审前）一律强制跑 `pnpm run first100:slice-gate`（= verify-module-graph + verify-export-jsdoc + verify-doc-budgets + verify-translation-pairing + constraints，五门无条件、不再人工判断"相关子集"）；触及 Cordis/typert 面的 slice 改跑 `pnpm run first100:slice-gate-cordis`（= 上述五门 + BLOCKED-013 的 cordis-catalog 三门）。起因：W3 首轮 3 lane 复审中 2 条（P0-05/P0-07）都漏 module-graph 门，与 BLOCKED-013（漏 cordis-catalog）、更早 P0-06.C/P（藏 11 处 lint 违规）同一根因类——"记得跑相关子集"是人的判断、会漏。**结构治本（待办，下个安全断点做，不得 mid-slice 加）**：把 lint、module-graph 这两个便宜且确定性的门折进 `.github/workflows/first100-exact-sha.yml`，让权威 CI 观察直接覆盖；执行前先确认 first100-exec 主线已对这两门干净、无隐藏债。BLOCKED-015（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：`generate-ledger.mjs`的`--candidate-sha`当前只校验 40-hex 正则格式，不校验其真实对应一个可解析的 git object——一次真实手误（格式合法但对象不存在的 SHA）证明了这个缺口，靠 Supervisor 自己 `git rev-parse` 交叉核对才逮到。**结构治本（待办，归入 BLOCKED-014 同批"下个安全断点"，不得 mid-slice 加）**：`--candidate-sha` 除格式正则外，加 `git cat-file -e <sha>^{commit}` 存在性校验 + reachability 校验（该 SHA 必须是 first100-exec 或其 attempt 分支的真实可达祖先，非任意游离 object）；`--ci-run-url` 内嵌 SHA 与 `--candidate-sha` 字面值一致性校验同批加入。**新增标准安排（2026-09-01，用户已授权 guanjieqiao-92）**：本会话若被分类器拦截 `git push`/`gh workflow` 等远端可逆操作，直接把"要推的分支/要触发的 SHA"发给 guanjieqiao-92，由其执行并回传 run ID——不必反复重试或长时间卡等；真正不可逆操作（force push 覆盖历史、动上游 master、删除、对外发布）仍必须回用户本人，谁都不能代劳。first100-exec 权威状态：详见 NOW 行最新叙述（本文件在多个 worktree 间可能存在滞后副本，以 first100-exec 分支上的版本为准）。**BLOCKED-016（硬约束，用户已定，2026-09-01）：基线重锚到上游 4e84901e 已确定，W4 开闸前必须先完成 BASE-ALIGN-v2（rebase 程序态+109/9 文件引用重验+8 已 accept epic 重验+23 PARTIAL 按 `spec/first100/sources/base-align-v2/23-partial-rescope-spec.md` 缩范围+9 P9 triage+继任 supervisor gate）；任何会话读到 W3 已关账，进 W4 前必须先重读 BLOCKED-QUEUE.md 的 BLOCKED-016 全文，不得凭 NOW 行摘要直接开闸。W3 本身不受影响、照常在 0a53fb55 完成。** BLOCKED-017（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01，防 timeout bump 变 reward-hack 后门）：调大测试超时仅当四条全满足才合法——①失败签名确系超时（runner 级 STACK_TRACE_ERROR/exceeded，非断言 expected/received diff）②测试确在做真实昂贵工作（非 hang、非该被逮住的性能回归）③新超时有依据（同文件/仓库已有先例，或实测时长×安全系数，非拍脑袋整数）④断言一条不减、不 skip，只动运行时预算。起因：批 CI 33568049055 真失败（P0-03.U 全量 workspace 扫描测试撞 5000ms 默认超时，同文件邻测已有 20_000 先例佐证），delegate 独立下载报告核实后放行本次修法、同时定此常设原则防今后滥用。P0-03 F 格/accept 复审时另评估全量真扫描的 CI 频率/性能是否需优化（非阻塞，见 BLOCKED-QUEUE.md#BLOCKED-017）。BLOCKED-018（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：ledger --accept 谓词(iii)（observation 互异性）改判据——由"两格须引用字节不同的 observation 文件"改为"两格可共享同一 observation/run，当且仅当①冻结命令（argv+case-titles 集合）确实不同 ②各格自身 case-titles 在其引用的 observation 内独立核实 present-and-passing"；仅当两格冻结命令完全相同时仍禁止共享。起因：exact-sha CI workflow 结构上每 run 只产出 2 份互异 observation 文件（签名 evidence bundle + 明文 vitest-report 上传），旧字节互异规则会把单 CI-run 批次硬顶在 2 格。追溯审计（delegate 直接要求，2026-09-01 已完成）：已用共享 observation 的 4 格（P0-03.C/P0-07.C @ run 33560054567；P0-03.U/P0-05.P @ run 33569804119）逐格核验 own case-titles 在其引用文件内 present+passing，结果全干净（zero missing, zero not-passing，四格均 OK）。**结构治本（待办，归入 BLOCKED-014 同批"下个安全断点"，不得 mid-slice 加）**：generate-ledger.mjs 加"per-cell case-titles present-in-observation"机械校验，去掉旧字节互异要求。BLOCKED-019（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：P0-05.F 的 hot-reload 鉴权门只挡"离开 enforce"（enforce→shadow/off），不挡 shadow→off——裁定合规，P0-05 acceptance 现状即满足，不用再动。修正后的理由：三态里只有 enforce 真强制，shadow/off 都不强制、对调用方应用同一个值；唯一掉保护的转换是离开 enforce，已挡；shadow→off 不减少任何强制。**跨 epic forward 约束（不动 P0-05，挂在未来消费 shadow 检测事件的 epic 身上）**：shadow 模式的 comparison/检测事件流现在关掉无需鉴权，今天没事——但若未来某 epic（P7 证据/eval 或监控类）把这些事件当作被依赖的安全信号，那个 epic 的 Reviewer 清单必须补上"禁用 shadow 前先鉴权"这条，仿 BLOCKED-011 point4 写法；现在不可执行、不改任何 registry acceptance 文本。BLOCKED-020（ANSWERED-BY-DELEGATE(gq-92), 2026-09-01）：批 CI（run 33576445894/33576210642，candidate 3724929ced）两次一致失败在 generate-specs.spec.ts 的 2 个用例（committed 派生产物与 fresh render 字节不一致）——delegate 独立查明根因：commit 7618f8da13 给 adjudication.json 加了第 8 条 manifest-patch（P0-03-F-agents-md-relocation，合法在案）但没重跑 generate-specs.ts 写模式，committed 的 manifest-v1.1.yaml 停留在 count:7、adjudication.json 已是 8，first100:slice-gate 不跑这道检查所以本地没抓到，全量 CI 才暴露；两 run 结论一致 → 确定性问题，不是 flake，未登记 flake-registry。已机械修复：node scripts/first100/generate-specs.ts 写模式重生成 3 个派生产物，--check 转绿，generate-specs.spec.ts 67/67，first100:slice-gate 干净，已提交待 delegate 重推+新 CI。**新常设规则**：今后任何改动 registry.json/adjudication.json 的 slice，Writer 交付前/Reviewer 复审前必须跑新增的 `pnpm run first100:slice-gate-registry`（=原 first100:slice-gate + 新增 first100:verify-specs＝generate-specs.ts --check，byte-drift fail-closed），不再用原 first100:slice-gate。BLOCKED-021（PARKED，ANSWERED-BY-DELEGATE(gq-92) 定 parking 条款，2026-09-02）：P2-01.C Writer 发现 must[1] 点名的 `ToolExecutionContext`/`SubagentRequest` 在当前代码库里都不存在，最接近的真实类型 `SubagentStartRequest`（packages/subagent/subagent/src/types.ts）不在 P2-01 任何阶段 files[] 里；C 格正确没碰。裁定：现在不裁，等 P2-01.U 或 BASE-ALIGN-v2（孰先）到，以当时 tip 的真实类型为证据解决（可能与 BASE-ALIGN-v2 已知的 subagent-seam 漂移——P5-10/result.ts 被上游 commit ec493c2db8 改过——同源，孰先到就并到哪边处理，不解两次）；届时必须走 deliverablePathPatch/rescope 明账+Reviewer 核，禁止 U 格 Writer 静默改瞄准。同会话另发现一条跟本项目无关的既有分支 `feat/p2-01-principal-identity`（用户自己 2026-08-22 的在制品，同路径旧实现，未合并进本程序任何分支）——Writer 和 Supervisor 均未触碰（独立重写、只当 prior art 读），delegate 已 FYI 用户+建议退休，删/留由用户一词定，不属 delegate/Supervisor 可裁范围。**BLOCKED-022（程序级硬约束，ANSWERED-BY-DELEGATE(gq-92), 2026-09-02）：P2-01 视同 P0-02 内核级安全地基——四格全绿、常规可跑 --accept 时，禁止 Supervisor 自行 --accept 收口，必须先发 delegate，delegate 裁定后取用户本人一词确认才放行；per-stage 正常流程不受影响，只挡最终 accept 那一步。** P2-01.C 首轮复审 REJECT（真发现）：isAdmin 是明文 public boolean，普通对象字面量可伪造 admin、编译通过、运行时 isAdminPrincipal 判真——本该复用同文件已用的 Branded<B> 不可伪造模式；另一处 package.json 的 main/exports/files 抄错模板指向不存在的 lib/index.js，真跑 tsdown build 会炸，tsc -b/vitest 测不出。已派 fix round，delegate 追加要求：isAdmin 修复须编译期+运行时双重验证、package.json 修复须真跑 tsdown build 确认。P1-01.C（Plugin Manifest v2）已交付 74/74，独立 Reviewer 复核中，delegate 追加要求：Writer 自创的 sideEffectClass/authAudience/allowedDestinations/dataClassification 词汇，Reviewer 须先查 packages/interaction/、packages/fs/fs-local policy、capability-seam/architecture.layers.json 有没有可复用既有分类，有则必须复用/扩展而非平行造系统（违反"建在既有 seam 上"铁律即升级为 REJECT 级发现）；若确属 greenfield，还要对下游未来消费方（P2 权限门等）的表达力验一遍，不只验今天的真实工具。**BLOCKED-024（程序级硬约束，ANSWERED-BY-DELEGATE(gq-92), 2026-09-02）：P0-07 最终 ACCEPT 升进"用户确认"档，与 P0-02 内核、P2-01 身份（BLOCKED-022）同级——理由：P0-07 本身就是整个 first100 账本的不可伪造完成门/反作弊地基，这道门若能绕过，ledger 里每个已 ACCEPTED 的绿格都站在可伪造的地基上；per-stage 正常流程不受影响，只挡最终 accept。P0-07.F 复审员必须亲手构造三个攻击（删必需 gate 整条 key+伪造自洽 signature；已堵的删 artifact key+删 manifest.json 回归；二者组合）而非读 Writer 自述。** P2-01.C 已进第三轮复审（第二轮 REJECT 两处真发现：①fix round 误删无关 tsconfig 别名破坏 doc-typecheck，已定位根因为该行错放在 gen-tsconfig-paths.ts 生成区间内、移出区间修复；②更深的漏洞——WeakSet 只证明 token 铸造过、未绑定具体 principal 身份，攻击者能拿偷来的合法 admin token 接到自己构造的、id/tenantId 完全匹配的 principal 上蒙混过关，因为 PrincipalId/TenantId 的 brandString 零运行时校验、任何人都能铸造匹配值——已改用 WeakMap<AdminGrant, 具体对象引用> + === 对象恒等比较而非字段值比较，这样字面量重建永远无法通过；诚实披露了序列化边界的已知局限）。P0-07.F 也在续作中，closing 一个真漏洞（删必需 gate 整条 key + 伪造自洽签名能骗过 accepted=true），援引本会话 P0-05.F 先例（F 格修早期阶段已验收文件里的真 bug 不算 BLOCKED-012 违规，此次更干净——verify-evidence.mjs 本就在 F 格声明 files[] 里）。**BLOCKED-025（PARKED，ANSWERED-BY-DELEGATE(gq-92), 2026-09-02）**：P2-01 admin-grant 的 === 对象恒等绑定跨进程/wire 边界会 fail-closed（反序列化出的 admin 对象 !== 原对象 → 真 admin 被拒，不是伪造者被放行，安全方向），这个设计本身被裁定为正确、不用改；但要求当场加一条永久回归测试证明这条 fail-closed 属性真被测过（structuredClone/JSON round-trip 出的合法 admin 必须被 isAdminPrincipal 判假），已派 addendum。并 PIN 了一条前向硬约束：未来任何要跨进程/wire 边界重建 principal 权威的 epic/阶段（疑似 P2-02 委托链/P6-08 会话持久化/P8-06 远程一带，不是既定项）必须用 Trust Kernel 签名+内核验签确立权威，绝不能信任反序列化字段、也不能照字段重铸 grant——那样会在边界重开同一个伪造洞；届时 Reviewer 必须亲手复现"反序列化伪造 admin"攻击。P1-01.C 独立 Reviewer ACCEPT（词汇确认真 greenfield，查过 packages/interaction/、fs-local、capability-seam 均无先例），另抓到 2 处未自报的真发现（assertJsonSerializable 不证明不可变性、JSON Schema 未闭合未知属性）已转成 README 已知局限追加、不阻塞，已合并冻结（first100-exec @ 2ca55e00ce）。**BLOCKED-023（ANSWERED-BY-DELEGATE(gq-92), 2026-09-02）**：flake-registry.json 证据标准新增第二条路径——同一 candidateSha 上 ≥2 个 CI attempt 且结果分歧（≥1 fail 且 ≥1 pass）与原有跨 SHA 标准并列可选；新增 scripts/first100/verify-flake-registry.ts 机械强制（同 SHA 全 fail 绝不满足、已用 scratch entry 验证确实拒绝）；check-capability-seams.spec.ts 的 acceptance[3] CLI 用例已按新标准登记（run 33578193549 attempt1 fail/attempt2 pass，两份 artifact 均已下载字节核验）。批 CI（run 33578193549 attempt2，17777 passed/0 failed，独立核实）已用于绿 P0-03.F/P0-05.U/P0-05.F/P0-07.P/P0-07.U（此次首次实战检验刚做的 BLOCKED-018 机制化——generate-ledger.mjs 原先仍强制字节互异旧规则，直接卡死批量绿格，已改造 usedObservationDigests/checkObservationDistinctness 按"冻结命令是否相同"判定，新增 Agent Note）。**P0-03（C+U+F，P=N/A）与 P0-05（C+P+U+F）现四格/三格全绿**，acceptance-coverage.json 已补 P0-03/P0-05 共 7 条 acceptance 索引映射，三谓词（覆盖闭合/候选链一致/观察互异）dry-run 均干净——**--accept 本身按 delegate 明确要求暂不执行，等其深核三谓词包后裁定**。first100-exec 现推进到 2c31b44799（P1-01.C、P2-01.C 均已合并+冻结，**但 ledger 里两者的 C 格仍是 NOT_RUN——合并冻结≠绿格，格子要真实 CI 观察后才能绿，二者要等下一批推 CI 才带上，别把"已合并"当"已完成"**）。P2-01.C 三轮复审后 ACCEPT，已合并冻结。**BLOCKED-026（硬门，ANSWERED-BY-DELEGATE(gq-92), 2026-09-02）**：P2-01.C 第三轮复审那条"readonly 无运行时强制、原地改字段绕过 admin 判定"发现，原判非阻塞（C 格不用改），但被 delegate 升级为**P2-01.U 复审员的硬门**——U 格接真实鉴权前，铸造 principal/grant 必须 Object.freeze + 加测试证明改字段被拒/不生效，不满足直接 REJECT，不接受"已披露局限"当理由放行。**待办（非阻塞，delegate 说顺手做即可）**：gen-tsconfig-paths 这个坑已连咬 3 次（P2-01 的 /types、P1-01 的 /types+/validate、都是手写子路径别名放进生成区间被静默丢），根治二选一——生成器支持任意子路径别名，或加一道"重生成后若原有别名消失就报错"的 fail-closed 后置检查；doc-typecheck 目前兜底能抓到，只是不够早。**安全事故（子代理用 git update-ref 强推共享分支）已独立验干净（delegate 确认 85145407c1 仍是 8cb0216f4d 祖先、无回卷、reflog 干净）**，P0-07.F supplement（BLOCKED-005）+ P1-01.P 均已合并冻结。**BLOCKED-027（ANSWERED-BY-DELEGATE(gq-92), 2026-09-02）**：P1-01.P 那处"比对只按能力身份、不比字段内容"的已知局限，挂到 P1-01.U 复审员清单——U 格接真实 enforcement 时必须重新判这是不是真洞（若 enforcement 依赖字段级一致就必须补，不许停在 known-limitation 不动）。P2-01.P Writer 已派（runtime-policy 层：跨租户 runtime 层拒绝，须避开 BLOCKED-021/025/026 三个边界，dispatch 里已写明不许碰共享分支指针）。first100-exec @ a4b8b22dc8。P2-01.P（assertRuntimeTenantPolicy 跨租户 runtime-policy 层 + AgentOptions.identity）首轮复审 REJECT（docs/subsystems/core.md 的 AgentOptions type-equiv 镜像漏同步，verify-type-equiv 同 node_modules 前后对比机械证实）→ BLOCKED-028 nonced sibling 分支（attempt/P2-01-P 被占）修复轮 → 二审 ACCEPT（独立复核 verify-type-equiv/test:docs/slice-gate-cordis/139 测试全绿，core.zh.md/i18n.yaml 联动修复经读 gate 脚本源码确认非可选）→ 已合并+冻结（commit a4b8b22dc8，argv 定 packages/identity/principal/tests/，expectCases 5 条本stage 专属 runtime-policy.spec.ts 标题，已跟真实 vitest --reporter=json dry-run 逐字核对）。principal 包缺 README（既存 C 格缺口）已记入 fix commit message 披露为 deferred，未夹带进本 slice。**下一批（P0-07.F supplement + P1-01.P + P2-01.P，三者均已合并冻结待 CI）等凑齐推**。已推：run 33596937698 @ 87585733cf，conclusion=success，17936/18004 passed 0 failed（delegate 独立核 + 本会话独立重复核：evidence-bundle.json 签名经 attest.ts --verify 通过、两份 vitest-report.json 摘要不同但 passing-title 集合相同、P1-01.P 20 条+P2-01.P 5 条 expectCases 逐字核对全present-and-passing、P0-07.F supplement.1 攻击③标题同样 present+passing）。已绿账：P1-01/P、P2-01/P、P0-07/F supplement.1（commit 388f73ed65；P0-07/F 主格保持不变，仅补 supplement 覆盖，不重绿）。**P0-07 --accept 由 delegate HOLD——BLOCKED-024 用户确认档，delegate 已两层核完（三谓词+攻击③进观察+灵敏度证明）正在向用户请求一词确认，用户确认后 delegate 会发"授权 --accept"，本会话届时才可跑，现在不得自行 --accept**。W3 三 lane 继续：已并行派发 P1-01.U（真 CLI plugin:verify + profile-boot 真强制 + 从活体 Cordis Context 构建 ObservedPluginCapabilities；带 BLOCKED-027 主动重判要求)、P2-01.U（真 agent-loop/session/sdk-protocol 身份接线；带 BLOCKED-026 Object.freeze 硬门+BLOCKED-025 若真跨 wire 边界须本 slice 内解决（已由 delegate 锐化：不许"分析后 defer"，跨界即必须本格解决，只有真进程内才维持 PARK）+BLOCKED-021 须用当下 tip 真实类型解决、禁止静默改瞄准），两者均已明确 scope out realTask 13 项证据包（E1/E2、S10/S11，留后续阶段/R10），后续两 Reviewer 均须核实这条 scope-out 有 registry stage 依据、非自定。**P1-01.U 已 ACCEPT+已合并冻结**（commit ce84c59a4f，first100-exec 现推进到此）：真 pre-mount admission（拒绝层的 patch 从不进 boot()）+真 post-mount quarantine（活体 Cordis Fiber 走查，Reviewer 独立从 vendored Cordis 源码复现 Entry.fiber 是 Object.create(rawFiber) 而 Impl.fiber 是原始 fiber、非同一引用但共享 uid 的机制），BLOCKED-027 关闭（逐一读过 ToolDefinition/Cordis Impl/MCP-client config/SkillDefinition 真实类型，确认字段级信息在活体注册里结构性不存在），DSH_PLUGIN_MANIFEST_ENFORCEMENT=enforce 默认关（无 shipped bundle 有真 manifest v2）。同槽 fast-follow（a1e9327f71）修正 3 处非阻塞披露缺口后 ACCEPT。argv 定 5 个新/扩展测试文件、expectCases 48 条本 stage 专属标题，已逐字核对。**P2-01.U 走到第 3 轮复审 REJECT（新发现：AgentPrincipal.delegatedBy 字段未被 samePrincipalIdentity/sameChainShape 任一检查覆盖，同类"身份变更未记日志"漏洞再现一次——第四轮修复已派发）**，**P2-01.U 经 4 轮复审（每轮抓到同一漏洞类"身份/权限相关变更未记日志"的一个更窄实例：轮1 AdminGrant 未冻结+身份互换未记日志；轮2 修复后被抓到只比终端 principal、不比链形；轮3 加 sameChainShape 后被抓到 AgentPrincipal.delegatedBy 仍未比；轮4 加 delegatedByOf 关闭，并用真实调用链证明 adminGrant 故意不比是安全的——resolveSessionIdentity 的真实生产调用方走 session 恢复的反序列化路径，直接对比会导致每次恢复 admin session 都误报）后第 4 轮复审 ACCEPT，已合并冻结（commit 59ca44d1ad，first100-exec 现推进到此）。P2-01 F 格：5 类真实攻击（并发/TOCTOU、手搭链绕过 extendChain、序列化边界租户伪造、循环/无界委托深度、anonymous-dev 冒充 admin）——4 类干净防住，1 类抓到真 bug：rootPrincipal/currentPrincipal/currentTenantId 在 empty-entries DelegationChain 上崩溃为不透明 TypeError，经 session-log 重放路径可触达（decodeStorageRecord 对非 chunk-row 事件零校验，identity/attached 正是此类），已修（assertNonEmptyChain 守卫）。首轮复审 REJECT（仅因缺 Agent Note，实质全部核实无误）→ 补 Agent Note 三件套 + intermediate-hop 披露（纯文档，零代码变更）→ 二轮复审 ACCEPT。P2-01 现四格（C/P/U/F）全部合并冻结（commit a87af6b3e0，first100-exec 现推进到此）；BLOCKED-022 仍挡最终 --accept，per-stage 流程不受影响。** P1-01.U 与 P2-01.U 均尚未推 CI（各自 merged+frozen，非 ledger green）。**BLOCKED-028（ANSWERED-BY-DELEGATE(gq-92), 2026-09-03，续 BLOCKED-003）**：机器级 git hook 方案已明确否决（超出程序委托域，影响本机全部共享此 repo 的 worktree/并发 session，已上交用户另裁，默认不装）；改用完全在范围内的 dispatch 层机械化——派发 fix/续作轮 Writer 前先 `git worktree list` 查目标分支是否被占用，占用则 dispatch 提示词里从一开始就分配带 nonce 的唯一 sibling 分支名（而非留给 Writer 撞见后临场发挥），未占用则照常派发；原有"禁 git update-ref/branch -f、撞见占用须建 sibling 分支"的 dispatch 级红线继续保留作第二层兜底。今后每次派发 fix/续作轮 Writer 一律执行本检查。
