{
  "schema": {
    "name": "first100-acceptance-coverage",
    "version": "1.0"
  },
  "entries": [
    {
      "epic": "P0-01",
      "acceptanceIndex": 0,
      "coveredBy": [
        {
          "stage": "C",
          "title": "canonicalizes the captured output: sorted keys, LF-only, UTF-8 NFC, POSIX-relative paths, and no nondeterministic fields"
        },
        {
          "stage": "F",
          "title": "captures a fingerprint that never leaks the fixture checkout's absolute path or backslash-spelled paths anywhere in its content"
        }
      ],
      "note": "acceptance[0]: 'same clean checkout produces the same canonicalized fingerprint on Linux and macOS.' No single CI job runs both OSes for one fingerprint, so this is evidenced structurally: canonicalization (sorted keys/LF-only/NFC/POSIX-relative paths) plus the absence of any absolute path or backslash-spelled value in the captured content together guarantee cross-platform identity by construction, not by running the same command twice on two OSes."
    },
    {
      "epic": "P0-01",
      "acceptanceIndex": 1,
      "coveredBy": [
        {
          "stage": "C",
          "title": "verify fails and names the minimal diff after a tracked bundle-row file drifts, and writes a rebase report"
        },
        {
          "stage": "F",
          "title": "verify fails and names the tampered SDK protocol types file"
        },
        {
          "stage": "F",
          "title": "verify fails and names the tampered known-event-types file"
        },
        {
          "stage": "F",
          "title": "verify fails and names the drifted workspace package manifest",
          "supplementSeq": 1
        }
      ],
      "note": "acceptance[1]: 'after modifying any key schema, bundle row, or package manifest, verify must fail and name the minimal diff.' Three sub-kinds named: schema (protocolSchemaHashes: SDK types + known-event-types, both F-stage), bundle row (cordis.patch.yml, C-stage), package manifest (workspacePackages, F-stage supplement -- BLOCKED-005; genuinely gapped until this supplement was added, honestly discovered while implementing BLOCKED-004 predicate (i))."
    },
    {
      "epic": "P0-01",
      "acceptanceIndex": 2,
      "coveredBy": [
        {
          "stage": "C",
          "title": "verify passes again once the drifted file is restored"
        }
      ],
      "note": "acceptance[2]: 'after restoring the file, verify must pass again.' Index-level, not per-drift-kind: the restore mechanism itself is generic (diffCapture re-runs against whatever is on disk), so one genuine restore-then-pass demonstration proves the mechanism for every drift kind alike -- consistent with how acceptance[0] is also evidenced generically rather than per-field."
    },
    {
      "epic": "P0-06",
      "acceptanceIndex": 0,
      "coveredBy": [
        {
          "stage": "F",
          "title": "replays snapshots/web/fresh-round-trip/session.jsonl (a real pre-schema-registry session fixture) with no negotiation failure and no silent event loss"
        },
        {
          "stage": "F",
          "title": "replays snapshots/sdk/text-turn/session.jsonl (a real pre-schema-registry session fixture) with no negotiation failure and no silent event loss"
        },
        {
          "stage": "F",
          "title": "replays snapshots/session/skill-load/session.jsonl (a real pre-schema-registry session fixture) with no negotiation failure and no silent event loss"
        }
      ],
      "note": "acceptance[0]: 'at minimum, able to read old session fixtures produced by the audit baseline.' Three real, pre-existing snapshots/*/session.jsonl fixtures (this repo's own keyless recorded-session corpus), all predating schema-registry's existence (no schemaVersion tag on any event), replayed through the real U-stage-wired negotiation path via a faithful envelope reconstruction (independently verified sound and non-vacuous by the Opus Reviewer against the codebase's own established repackSessionSnapshot reverse-direction logic)."
    },
    {
      "epic": "P0-06",
      "acceptanceIndex": 1,
      "coveredBy": [
        {
          "stage": "U",
          "title": "rejects an initialize handshake whose explicit schemaVersion major differs from the registered major"
        },
        {
          "stage": "U",
          "title": "rejects a session-event line whose explicit schemaVersion major differs from the registered major"
        },
        {
          "stage": "U",
          "title": "does not swallow a schema-incompatible line into the tolerant corrupt-suffix heuristic"
        },
        {
          "stage": "U",
          "title": "rejects registration when the stored section declares an incompatible major"
        },
        {
          "stage": "U",
          "title": "keeps the last good value (no silent field loss) when a hot-reloaded section declares an incompatible major"
        }
      ],
      "note": "acceptance[1]: 'incompatible client gets machine-readable error, no silent field loss.' Structured SchemaCompatibilityError (schemaId, encountered/registered versions) asserted at all 3 real U-stage boundaries (SDK initialize, session replay, settings load); explicit non-swallowing proven at the replay boundary (corrupt-suffix heuristic doesn't absorb a real negotiation failure) and no-silent-field-loss proven at the settings hot-reload boundary (last good value retained, no partial commit)."
    },
    {
      "epic": "P0-06",
      "acceptanceIndex": 2,
      "coveredBy": [
        {
          "stage": "P",
          "title": "round-trips forward then backward without loss: {\"firedAt\":\"2026-01-01T00:00:00.000Z\"}"
        },
        {
          "stage": "P",
          "title": "round-trips backward then forward without loss: {\"occurredAt\":\"2026-01-01T00:00:00.000Z\"}"
        },
        {
          "stage": "P",
          "title": "a naive reverse (splitting fullName on the first space) does not recover the original data for an ambiguous name -- proof of genuine information loss"
        },
        {
          "stage": "P",
          "title": "exports no reverse migration function for the name merge -- explicit acknowledgment of irreversibility, not a silent absence"
        }
      ],
      "note": "acceptance[2]: 'all registry migrations have bidirectional or explicit-irreversible tests.' Two illustrative migration examples cover both classes: renameFiredAtToOccurredAt/renameOccurredAtToFiredAt is proven bidirectional (real round-trip, both directions, no loss); mergeNameFields is proven explicitly irreversible (a real pigeonhole information-loss counter-example, independently verified by the P-stage Opus reviewer to generalize, plus no reverse export -- an honest acknowledgment, not a silent gap)."
    },
    {
      "epic": "P0-02",
      "acceptanceIndex": 0,
      "coveredBy": [
        {
          "stage": "U",
          "title": "rejects a plugin that tries to override the pinned kernel via a second ctx.provide -- never a replaceable Cordis Service (must[3])"
        },
        {
          "stage": "F",
          "title": "leaves the kernel present after disposing an unrelated plugin, and shows the kernel is owned by the root fiber, not a plugin fiber"
        },
        {
          "stage": "F",
          "title": "rejects a plugin row applied to the root Include entry after boot exactly as it rejects one present at initial boot"
        },
        {
          "stage": "F",
          "title": "rejects delete-then-reprovide against a kernel pinned via pinTrustKernel, and leaves the original kernel pinned"
        },
        {
          "stage": "F",
          "title": "rejects a direct reassignment of the store entry (no delete) against a kernel pinned via pinTrustKernel, and leaves the original kernel pinned"
        },
        {
          "stage": "F",
          "title": "rejects impl.value = forged against a kernel pinned via pinTrustKernel, and leaves ctx.get('trustKernel') unforged"
        },
        {
          "stage": "F",
          "title": "rejects a direct write to ctx.root.fiber.store['trustKernel'], and leaves both ctx.get and ctx.trustKernel property access unforged for an unrelated sibling"
        },
        {
          "stage": "F",
          "title": "rejects a direct write to ctx.reflect.props['trustKernel'], and leaves ctx.trustKernel property access unforged"
        }
      ],
      "note": "acceptance[0]: '任意插件卸载、覆盖 service 或动态 mount 都不能替换 kernel policy/audit/signature verifier' (no plugin unload, service override, or dynamic mount can replace the kernel's policy/audit/signature verifier). Covered for ctx.get('trustKernel') and the root context's own direct property read in every tested vector (delete-then-reprovide, direct store reassignment, Impl mutation, root-fiber-store key, reflect.props key, unload, dynamic mount). Per spec/first100/exec/BLOCKED-QUEUE.md's BLOCKED-011 final resolution (ANSWERED-BY-USER, 2026-09-01): 3 further property-access-only residual vectors (self-fiber poisoning, ancestor-fiber poisoning/vector G, wholesale root-store replacement/vector H, all in pin-hardening.spec.ts) are independently proven currently unreachable by any real consumer (exhaustive grep: zero real code anywhere reads ctx.trustKernel via property access; the sole real consumer, apps/cli/src/profile-boot.ts:314, uses ctx.get exclusively) and are closed to real code by the CI-enforced scripts/verify-trust-kernel-property-access.ts gate, not eliminated at the Cordis mechanism level. Clause 0's real security intent (no enforcement-point-reachable bypass -- there is currently no real enforcement point to bypass at all) is accepted as satisfied on this basis, with a new standing hard-trigger rule requiring the vendored Cordis Fiber structural fix (Option A) before any future epic wires a real enforcement point (~W6)."
    },
    {
      "epic": "P0-02",
      "acceptanceIndex": 1,
      "coveredBy": [
        {
          "stage": "C",
          "title": "keeps TrustKernelPolicyQuery.payload exactly `unknown` -- no business-domain field on the kernel surface"
        },
        {
          "stage": "C",
          "title": "keeps TrustKernelAuditEntry.payload exactly `unknown` -- no business-domain field on the kernel surface"
        },
        {
          "stage": "C",
          "title": "keeps TrustKernelSandboxAttestation.payload exactly `unknown` -- no business-domain field on the kernel surface"
        },
        {
          "stage": "C",
          "title": "exports no Config schema and no apply(ctx, config) plugin entry -- nothing here is a Cordis plugin export"
        },
        {
          "stage": "U",
          "title": "denies and rejects by default -- no concrete policy/attestation provider exists yet (spec/trust-kernel.md acceptance clause 2)"
        }
      ],
      "note": "acceptance[1]: 'Kernel API 无模型可见文本、无业务领域逻辑、无具体 provider 实现' (the kernel API carries no model-visible text, no business-domain logic, no concrete provider implementation). No-business-domain-field proven at the 3 opaque-payload members (C-stage); no-concrete-provider proven by the deny/reject/no-op defaults (U-stage, createTrustKernel); no-model-visible-text is structural (the 6 capability members carry no prompt/schema text at all, per spec/trust-kernel.md acceptance clause 2, and the package's own README Model Experience section states zero direct token/KV-cache effect)."
    },
    {
      "epic": "P0-02",
      "acceptanceIndex": 2,
      "coveredBy": [
        {
          "stage": "U",
          "title": "fails closed (throws, never warns) when uninitialized without the insecure opt-in"
        }
      ],
      "note": "acceptance[2]: '未初始化 kernel 时，生产 profile 必须 fail closed' (an uninitialized kernel must fail closed in a production profile)."
    },
    {
      "epic": "P0-02",
      "acceptanceIndex": 3,
      "coveredBy": [
        {
          "stage": "U",
          "title": "warns and continues (never throws) when uninitialized with the insecure opt-in -- every call, a permanent warning, not a one-time notice"
        },
        {
          "stage": "U",
          "title": "stays opted out when the switch is unset or empty"
        },
        {
          "stage": "U",
          "title": "opts in on ANY non-empty value, including falsy-looking ones"
        },
        {
          "stage": "U",
          "title": "defaults to a stderr write when no warn sink is supplied"
        }
      ],
      "note": "acceptance[3]: '开发 profile 可显式启用 insecure 模式并显示永久警告' (a development profile may explicitly enable an insecure mode, displaying a permanent warning). The DSH_TRUST_KERNEL_INSECURE opt-in resolver (stays opted out by default, opts in on any non-empty value) plus the permanent (not one-time) warning behavior are both proven directly."
    }
  ]
}
