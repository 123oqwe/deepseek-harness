{
  "schema": { "name": "first100-command-freeze", "version": "1.0" },
  "entries": [
    {
      "epic": "P0-01",
      "stage": "C",
      "argv": ["pnpm", "exec", "vitest", "run", "tests/release/baseline-fingerprint.spec.ts", "--reporter=json"],
      "expectExit": 0,
      "expectCases": [
        "captures .dsh/baseline.json with the canonical field set the MUST clause requires",
        "canonicalizes the captured output: sorted keys, LF-only, UTF-8 NFC, POSIX-relative paths, and no nondeterministic fields",
        "writes the audit SHA into both the machine file and docs/audit/baseline-fingerprint-<sha>.md",
        "verify exits 0 against an unmodified captured baseline",
        "verify fails and names the minimal diff after a tracked bundle-row file drifts, and writes a rebase report",
        "verify passes again once the drifted file is restored"
      ],
      "files": ["tests/release/baseline-fingerprint.spec.ts"],
      "frozenAtUtc": "2026-08-31T22:35:48Z",
      "dryRunProof": {
        "treeSha": "65d47643489999a8a23888359a6ce0537821d733",
        "testsDiscovered": 6
      },
      "note": "B4(d) dry-run proof captured on attempt/P0-01-C at commit 19e2afcd7b6da9c6f73ab3aa18ca7b75141a1992 (pre-GREEN: only the RED scaffold -- .dsh/baseline.json specimen, the contract test, the patched-path doc specimen, and scripts/release/baseline-fingerprint.mjs's B4(f)/B5 no-behavior stub -- committed; real capture/verify logic is a separate later GREEN slice). expectExit=0/all 6 expectCases passing is the GREEN target this freeze grades against; today's real state is 6/6 failing on genuine behavioral mismatches (see RED observation), never MODULE_NOT_FOUND/SyntaxError/is-not-defined. Deliverable path patched per BLOCKED-001 (tests/first100/adjudication.json deliverablePathPatches.entries.P0-01): the doc target is docs/audit/baseline-fingerprint-0a53fb55bea101816fa226bb964ae2bed71c343b.md, not the registry's byte-locked docs/audit/baseline-b150a551.md."
    },
    {
      "epic": "P0-01",
      "stage": "F",
      "argv": ["pnpm", "exec", "vitest", "run", "tests/release/baseline-fingerprint.spec.ts", "--reporter=json"],
      "expectExit": 0,
      "expectCases": [
        "verify fails and names the tampered SDK protocol types file",
        "verify fails and names the tampered known-event-types file",
        "verify fails and names the tampered pnpm-lock.yaml",
        "captures a fingerprint that never leaks the fixture checkout's absolute path or backslash-spelled paths anywhere in its content"
      ],
      "files": ["tests/release/baseline-fingerprint.spec.ts"],
      "frozenAtUtc": "2026-09-01T00:13:38Z",
      "dryRunProof": {
        "treeSha": "baa9f00d57363a4e9fc14b4ad092834cba88ee5e",
        "testsDiscovered": 10
      },
      "note": "F-stage extends the same tests/release/baseline-fingerprint.spec.ts file the (P0-01, C) entry above already froze -- closes P0-01's validation clause gap (tamper cordis.patch.yml/SDK-types/event-types separately, all three drift kinds caught; the C-stage slice only exercised cordis.patch.yml) plus a genuine cross-platform structural case. argv is identical to the C-stage entry but binds a DIFFERENT (epic, stage) pair with its own distinct expectCases and its own dry-run proof against the tree at commit 46aa892d6fc5b2c8ac0a612390b3938f5e5b10fb (10 tests discovered, the 6 original C-stage cases plus these 4) -- greening this entry will use a separate CI run's observation report (different candidate SHA, different report bytes), never the (P0-01, C) cell's already-recorded report, per B7(1)'s no-shared-observation-file rule. Real partial RED at this tree: 9/10 pass, 1 genuinely fails (\"verify fails and names the tampered pnpm-lock.yaml\", real AssertionError, not a module/syntax error) because the implementation does not yet track pnpm-lock.yaml; the other 3 new cases already pass against the unmodified C-stage implementation (legitimate coverage completion, not fabricated red -- see BLOCKED-QUEUE.md-adjacent commit history for the honest RED/already-green breakdown per case)."
    },
    {
      "epic": "P0-01",
      "stage": "U",
      "argv": ["pnpm", "exec", "vitest", "run", "tests/first100/fixtures/P0-01.composition.spec.ts", "packages/guard/baseline-preflight/tests/apply.spec.ts", "--reporter=json"],
      "expectExit": 0,
      "expectCases": [
        "boots a clean fixture normally through the real Loader composition",
        "aborts startup with the drifted path when the fixture is tampered after capture",
        "no-ops when the checkout has no captured baseline",
        "resolves without throwing against an unmodified captured baseline",
        "throws naming the drifted path when the fixture is tampered after capture"
      ],
      "files": ["tests/first100/fixtures/P0-01.composition.spec.ts", "packages/guard/baseline-preflight/tests/apply.spec.ts"],
      "frozenAtUtc": "2026-09-01T02:24:56Z",
      "dryRunProof": {
        "treeSha": "c429f4d4dbac905ffcc76ab0a90cb9932847e0ad",
        "testsDiscovered": 5
      },
      "note": "U-stage (Usage/Composition): a new package @deepseek-ai/dsh-baseline-preflight (packages/guard/baseline-preflight) wires the C/F-stage-frozen scripts/release/baseline-fingerprint.mjs's verify check into a real boot-time Cordis plugin, satisfying P0-01's MUST clause (verify before any execution batch begins) via a genuine end-to-end round trip (maintainer directive B7(2): real registration + no-op provider is an explicit merge blocker) -- tests/first100/fixtures/P0-01.composition.spec.ts boots a real minimal Loader composition through @deepseek-ai/dsh-app-boot via packages/test-support/loader-smoke and asserts real process exit codes/stderr; packages/guard/baseline-preflight/tests/apply.spec.ts covers the no-baseline and no-drift branches directly. Dry-run proof captured on attempt/P0-01-U at commit cf63a52ba493e6b4771cdc947816e3a2aae2b4c7 (RED scaffold: the two new test files, the new package's scaffold, and a B4(f)/B5 no-behavior apply() stub -- real drift-check implementation and product wiring, packages/bundle/base/cordis.patch.yml and scripts/release/baseline-fingerprint.mjs's shared verifyBaseline export, are a separate later GREEN commit). Real partial RED at this tree: 3/5 pass (no-baseline no-op and clean-boot cases, trivially true against any implementation), 2/5 genuinely fail on real behavioral mismatches (boot never aborts on drift; apply never rejects on drift) -- never MODULE_NOT_FOUND/SyntaxError. Independent verification (2026-09-01) confirmed the shared-base-bundle row ships `disabled: true` (matching the existing `hmr` row's opt-in-per-profile precedent) so this does not collide with this repo's own committed .dsh/baseline.json specimen and does not abort real `pnpm dsh` boots from this checkout -- confirmed directly by running apps/cli/tests/profiles/headless/tests/headless.expected.e2e.ts (12/12 passed) with the row disabled."
    },
    {
      "epic": "P0-01",
      "stage": "P",
      "argv": ["pnpm", "exec", "vitest", "run", "tests/release/baseline-fingerprint.spec.ts", "--reporter=json"],
      "expectExit": 0,
      "expectCases": [
        "wires baseline:capture/baseline:verify in package.json to the real script, and pnpm run actually executes it"
      ],
      "files": ["tests/release/baseline-fingerprint.spec.ts"],
      "frozenAtUtc": "2026-09-01T03:17:21Z",
      "dryRunProof": {
        "treeSha": "dc1b03c51f7283bcafcd0423501a7508edcf8257",
        "testsDiscovered": 11
      },
      "note": "BLOCKED-002 (2026-09-01, extended-trigger reading): P-stage declares two files in the wave-map (scripts/release/baseline-fingerprint.mjs, package.json), no dedicated test file of its own. scripts/release/baseline-fingerprint.mjs's coverage was already proven repeatedly by the (P0-01, C) and (P0-01, F) entries' cases (all spawn it directly). checkSharedStageCoverage() (scripts/first100/generate-specs.ts) mechanically confirmed this AND found a real gap: package.json was only ever an incidental fixture-scaffolding string in the existing tests, never exercising its actual baseline:capture/baseline:verify npm-script role. This entry's one new expectCase closes that gap with dedicated evidence (real package.json script-string assertion + a real `pnpm run baseline:capture`/`baseline:verify` invocation against an isolated fixture) -- P-stage now has genuine, direct evidence of its own, so this is an ordinary single-cell entry (no coveredStages needed): the (P0-01,P) ledger cell's greening rests on this entry's own observation, informed by (not borrowing the observation of) the already-recorded (P0-01,C)/(P0-01,F) evidence for the other declared file. Real RED at this tree (commit 1d959cd14a1147f16760178fef2125136512bc0d, package.json's two script lines temporarily removed for this commit only): 10/11 pass, 1 genuinely fails (`expected undefined to be 'node scripts/release/baseline-fingerprint.mjs capture'`), never a module/syntax error; package.json's real content is restored in the following GREEN commit."
    }
,
    {
      "epic": "P0-01",
      "stage": "F",
      "supplements": {
        "epic": "P0-01",
        "stage": "F"
      },
      "supplementSeq": 1,
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "tests/release/baseline-fingerprint.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "verify fails and names the drifted workspace package manifest"
      ],
      "files": [
        "tests/release/baseline-fingerprint.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T05:20:00Z",
      "dryRunProof": {
        "treeSha": "098b028a224a49f6f3877d102152372e89bd0c48",
        "testsDiscovered": 12
      },
      "sensitivityProof": {
        "mutationDescription": "Locally (never committed) neutralized diffCapture()'s workspacePackages comparison branch in scripts/release/baseline-fingerprint.mjs (the `if (JSON.stringify(stored.workspacePackages) !== JSON.stringify(fresh.workspacePackages)) { drift.push(...) }` block), then re-ran only the new case against the fixture that renames packages/alpha/package.json's name after capture.",
        "failureSummary": "AssertionError: expected +0 not to be +0 // Object.is equality, at tests/release/baseline-fingerprint.spec.ts:216:37 (expect(verifyResult.status).not.toBe(0)) -- verify wrongly exited 0 (no drift detected) once the covered behavior was disabled, confirming the new case is not vacuously true. Mutation reverted immediately after capturing this; git diff --stat on the script was empty before continuing. Independently reproduced by a fresh isolated Reviewer (BLOCKED-003-compliant dispatch, non-isolated worktree, branch/HEAD invariant verified unchanged before and after) with the exact same failure output, plus a SECOND, independent mutation (weakening the comparison to array-length-only, which the fixture's same-count rename would defeat under a naive implementation) -- also caught by the new case with the same genuine failure. Reviewer verdict: ACCEPT."
      },
      "note": "BLOCKED-005 supplement (2026-09-01): closes the real, honestly-discovered gap in P0-01's acceptance[1] coverage -- schema (F-stage) and bundle-row (C-stage) drift were already tested, but package-manifest drift (a workspace package.json's name changing, tracked via workspacePackages in diffCapture()) never had a named case, even though the implementation already handles it. Coverage-completion path (behavior already exists; genuine RED against the unmodified tree is unobtainable) -- the mandatory sensitivityProof above is the substitute per the BLOCKED-005 hardening clause. Never re-greens or invalidates the primary (P0-01, F) entry's own already-GREEN cell; only contributes this case's title into BLOCKED-004 predicate (i)'s coverage-closure union, recorded in spec/first100/exec/acceptance-coverage.json's acceptanceIndex 1 citation."
    }
,
    {
      "epic": "P0-06",
      "stage": "C",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "packages/schema/schema-registry/tests/compatibility.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "registers every known session-event payload type at 1.0",
        "registers the named SDK protocol wire types at 1.0",
        "exposes at least every bootstrapped schema through listSchemas()",
        "registers a schema at its declared first version with the given migration",
        "rejects a duplicate registration under the same schemaId, never silently replacing it",
        "rejects an invalid first version {\"major\":0,\"minor\":0}",
        "rejects an invalid first version {\"major\":1,\"minor\":-1}",
        "rejects an invalid first version {\"major\":1.5,\"minor\":0}",
        "accepts an additive-only change set that bumps only minor",
        "rejects an additive-only change set that bumps major",
        "accepts a breaking change set that bumps major by exactly 1 and resets minor to 0",
        "rejects a breaking change set that bumps major but does not reset minor to 0",
        "rejects an additive change set that does not increase minor",
        "rejects a breaking change set that only bumps minor",
        "rejects a breaking change set that skips a major version",
        "rejects evolution declaring no changes",
        "rejects evolving a schema that was never registered",
        "reports compatible for an encountered version sharing the registered major, regardless of minor",
        "returns a structured SchemaCompatibilityError, never a bare string, for a major mismatch",
        "returns a structured SchemaCompatibilityError for an unregistered schemaId, never silently accepting it"
      ],
      "files": [
        "packages/schema/schema-registry/tests/compatibility.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T06:10:00Z",
      "dryRunProof": {
        "treeSha": "6fc6b61133abb103ad20b39326b3b7ae5b89cd38",
        "testsDiscovered": 20
      },
      "note": "P0-06 C-stage (2026-09-01): real registerSchema/evolveSchema/negotiateSchema/getSchema/listSchemas for @deepseek-ai/dsh-schema-registry -- versions only individual persisted/wire-protocol leaf objects (session-event payload shapes, sdk-protocol wire types), never the session log's own container format (SESSION_FORMAT_VERSION stays untouched, zero references), per maintainer decision BLOCKED-008's scope-split ruling (reading (a)). evolveSchema enforces must[2]/must[3]: additive-only changes bump minor strictly, any breaking change bumps major by exactly 1 and resets minor to 0. negotiateSchema returns a structured SchemaCompatibilityError (never a bare string, never silent field loss) per acceptance[1]. packages/core/session/src/known-event-types.ts is left untouched (fully generated, byte-exact --check gate, zero extension point) -- the registry instead bootstraps from its real KNOWN_SESSION_EVENT_TYPES export read-only, which cannot drift from the generated catalog by construction. packages/sdk/protocol/src/types.ts gains schemaId+version doc comments on every RPC request/result/notification payload type (nested field types like SdkEncodedImageBlock ride their container's schemaId, not separately registered). Real RED at commit b89d9924c2 (stub types.ts/index.ts): 18/18 failed, all genuine on-topic assertion/compiler-diagnostic failures, never module/syntax errors. Went through two rounds of Opus-level adversarial review (mandatory for this L1_CONTRACT epic per C1): round 1 found 2 real mutation-survivor gaps in evolveSchema's version-bump enforcement (a breaking bump not resetting minor to 0, and a no-op additive bump both went undetected by the then-18-test suite) plus a doc overclaim; round 2's fix closed both gaps (independently mutation-replayed before commit) but itself introduced fresh baseline drift by editing a tracked file without recapturing -- caught by a second review pass and fixed in a third commit. Final ACCEPT confirmed via a scoped third review. P0-01's .dsh/baseline.json was re-captured (this slice edits a baseline-tracked file); pnpm baseline:verify shows zero content drift, only the structurally-unavoidable self-referential gitSha exception at the exact recapture commit (same pattern already present in P0-01's own baseline commits)."
    }
,
    {
      "epic": "P0-02",
      "stage": "C",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "packages/kernel/trust-kernel/tests/boundary.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "imports nothing: the kernel type surface depends on no other package",
        "has no runtime code: every top-level statement is a type/interface declaration or an ambient declare-const symbol",
        "declares an exported TrustKernel interface with exactly the six must[2] members, all readonly, and no methods",
        "exports no Config schema and no apply(ctx, config) plugin entry -- nothing here is a Cordis plugin export",
        "brands TrustKernelRootIdentity as an opaque interface, never a plain type alias",
        "brands TrustKernelSignatureRoots as an opaque interface, never a plain type alias",
        "brands TrustKernelSecretBrokerHandle as an opaque interface, never a plain type alias",
        "keeps TrustKernelPolicyQuery.payload exactly `unknown` -- no business-domain field on the kernel surface",
        "keeps TrustKernelAuditEntry.payload exactly `unknown` -- no business-domain field on the kernel surface",
        "keeps TrustKernelSandboxAttestation.payload exactly `unknown` -- no business-domain field on the kernel surface",
        "TrustKernelRootIdentity's brand symbol TRUST_KERNEL_ROOT_IDENTITY carries no export modifier",
        "TrustKernelSignatureRoots's brand symbol TRUST_KERNEL_SIGNATURE_ROOTS carries no export modifier",
        "TrustKernelSecretBrokerHandle's brand symbol TRUST_KERNEL_SECRET_BROKER carries no export modifier",
        "gives TrustKernelRootIdentity exactly one member, a computed property keyed by TRUST_KERNEL_ROOT_IDENTITY",
        "gives TrustKernelSignatureRoots exactly one member, a computed property keyed by TRUST_KERNEL_SIGNATURE_ROOTS",
        "gives TrustKernelSecretBrokerHandle exactly one member, a computed property keyed by TRUST_KERNEL_SECRET_BROKER",
        "exports none of the three branding symbols by name, per the real compiler's own module-exports list",
        "type-checks a well-formed TrustKernel usage with zero diagnostics",
        "rejects reassigning a TrustKernel member -- every capability is readonly",
        "rejects constructing an opaque handle from an object literal -- no legitimate value exists outside an unsafe cast",
        "names models among what remains a plugin",
        "names tools among what remains a plugin",
        "names storage providers among what remains a plugin",
        "names workflow among what remains a plugin",
        "names memory provider among what remains a plugin",
        "names ui among what remains a plugin",
        "names root identity among what is never a plugin",
        "names deny enforcement among what is never a plugin",
        "names audit-chain root among what is never a plugin",
        "names signature-verification root among what is never a plugin",
        "cross-references the TrustKernel type surface and the ctx.provide/ctx.plugin distinction",
        "documents the auditAppend capability",
        "documents the policyEnforcement capability",
        "documents the rootIdentity capability",
        "documents the sandboxAttestationVerifier capability",
        "documents the secretBroker capability",
        "documents the signatureRoots capability",
        "cross-references the boundary doc and this slice's own test file"
      ],
      "files": [
        "packages/kernel/trust-kernel/tests/boundary.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T06:40:00Z",
      "dryRunProof": {
        "treeSha": "552da8935430cd9fa1505cdf744d776c76beeb63",
        "testsDiscovered": 38
      },
      "note": "P0-02 C-stage (2026-09-01): the TrustKernel narrow, unforgeable capability-surface type contract for @deepseek-ai/dsh-trust-kernel. Real RED at commit e95ca9e584 (stub types.ts exporting only a placeholder interface): 10/31 failed, all genuine TypeScript compiler diagnostics/structural mismatches, never module/syntax errors. Went through THREE rounds of Opus-level adversarial review (mandatory for this L0_KERNEL epic per C1): round 1 found a real gap -- the suite proved the surface was narrow and readonly but never actually tested unforgeability (2 of 5 seeded mutations, exporting a brand symbol or replacing it with an ordinary field, survived); fixed with 7 new tests (31->38), independently mutation-replayed twice before/after by two separate reviewers. Round 2 found the fix's honest package.json correction (dropping phantom main/types fields pointing at a lib/index.js this slice didn't yet produce) broke the repo-wide check-workspace-constraints.ts gate -- routed to the delegate as BLOCKED-009 (a brand-new package's C-stage-only slice structurally can't satisfy that gate without an index.ts). Delegate ruling: a pure-type-only src/index.ts (export type re-export only, zero runtime/Cordis surface) is now mandatory B4(f) scaffold for every future new-package C-stage slice, not a one-off. Round 3 found docs/module-graph (a peerDependencies-derived generated artifact) still stale on this package's own edge -- regenerated. Final ACCEPT confirmed via self-verification against the unmodified, already-reviewed gate scripts (typecheck, check-workspace-constraints.ts, verify-module-graph, publint, vitest all clean). A real, unrelated, pre-existing defect from P0-01.U's already-ACCEPTED slice was surfaced along the way (verify-cordis-config: packages/bundle/base/cordis.patch.yml references @deepseek-ai/dsh-baseline-preflight without declaring it in packages/bundle/base/package.json's dependencies) -- noted, not fixed here, out of scope for this epic."
    }
,
    {
      "epic": "P0-02",
      "stage": "U",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "packages/kernel/trust-kernel/tests/boot.spec.ts",
        "apps/cli/tests/trust-kernel-posture.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "stays opted out when the switch is unset or empty",
        "opts in on ANY non-empty value, including falsy-looking ones",
        "is a no-op when initialized, opt-in or not",
        "fails closed (throws, never warns) when uninitialized without the insecure opt-in",
        "warns and continues (never throws) when uninitialized with the insecure opt-in -- every call, a permanent warning, not a one-time notice",
        "defaults to a stderr write when no warn sink is supplied",
        "constructs a deep-frozen TrustKernel with all six members",
        "denies and rejects by default -- no concrete policy/attestation provider exists yet (spec/trust-kernel.md acceptance clause 2)",
        "constructs a fresh value on every call -- the one process-lifetime pin is the caller's discipline (a single ctx.provide), not a module-level singleton",
        "pins the kernel before any config-tree entry mounts -- a plugin sees it already present",
        "rejects a plugin that tries to override the pinned kernel via a second ctx.provide -- never a replaceable Cordis Service (must[3])"
      ],
      "files": [
        "packages/kernel/trust-kernel/tests/boot.spec.ts",
        "apps/cli/tests/trust-kernel-posture.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T07:35:00Z",
      "dryRunProof": {
        "treeSha": "aaf0f3c5132ec2b0c6567b52a59ec15d0569b0e8",
        "testsDiscovered": 11
      },
      "note": "P0-02 U-stage (2026-09-01): real boot-sequence integration. createTrustKernel() (src/index.ts) replaces the C-stage pure-type stub (BLOCKED-009) with a deep-frozen TrustKernel construction. apps/cli/src/profile-boot.ts's runProfile() constructs the kernel BEFORE calling boot() at all (boot() itself creates the Cordis Context), then the existing prepare closure pins it via ctx.provide('trustKernel', kernel) -- never ctx.plugin(...). New DSH_TRUST_KERNEL_INSECURE opt-in + enforceTrustKernelPosture realize acceptance clauses 3/4 (production fail-closed / dev insecure-opt-in warning). Real RED at commit 2b2085a409 (stub createTrustKernel/posture functions): 6/17 failed genuinely (unfrozen kernel, wrong verdicts, opt-in resolver always false), never module/syntax errors. Opus-level adversarial review (mandatory for this L0_KERNEL epic): verified Cordis's real ReflectService.provide (vendor/cordis/src/reflect.ts) genuinely throws on a second provide() for the same key -- must[3]'s 'never replaceable' guarantee is proven against real, unmodified Cordis behavior, not a wrapper this package invented. 6 seeded mutations (deep-freeze removal at 2 depths, fail-closed no-op, insecure-opt-in-always-true, pin-deletion, malicious-plugin-target-neutering) all caught. Reviewer's own extra bypass probes (ctx.set() second-write, direct mutation of the frozen value, ctx.isolate() shadowing) found no exploitable bypass of the pinned value's identity, though isolation-scope shadowing is noted as worth an explicit doc sentence once real capability providers land (non-blocking). Reviewer additionally proved via mutation D1 (deleting the ctx.provide pin) that the fail-closed throw and the insecure-opt-in warning are BOTH reachable end-to-end through the real dsh subprocess boot (apps/cli/tests/profiles/headless/tests/headless.expected.e2e.ts, real DSH_TRUST_KERNEL_INSECURE env override) -- overturning the Writer's own more pessimistic self-assessment that the fail-closed branch was unreachable through real wiring. A dedicated committed e2e test for these two integration paths (today only mutation-observable) is noted as a non-blocking follow-up."
    }
,
    {
      "epic": "P0-06",
      "stage": "P",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "packages/schema/schema-registry/tests/migration.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "exercises the real registry: evolveSchema accepts the breaking rename and stores the forward migration",
        "renames firedAt to occurredAt, carrying the value across unchanged",
        "round-trips forward then backward without loss: {\"firedAt\":\"2026-01-01T00:00:00.000Z\"}",
        "round-trips forward then backward without loss: {\"firedAt\":\"2026-08-31T12:34:56.789Z\",\"turn\":3,\"step\":1}",
        "round-trips backward then forward without loss: {\"occurredAt\":\"2026-01-01T00:00:00.000Z\"}",
        "round-trips backward then forward without loss: {\"occurredAt\":\"2026-08-31T12:34:56.789Z\",\"turn\":3,\"step\":1}",
        "exercises the real registry: evolveSchema accepts the breaking merge and stores the forward migration",
        "merges firstName and lastName into a single space-separated fullName",
        "a naive reverse (splitting fullName on the first space) does not recover the original data for an ambiguous name -- proof of genuine information loss",
        "exports no reverse migration function for the name merge -- explicit acknowledgment of irreversibility, not a silent absence"
      ],
      "files": [
        "packages/schema/schema-registry/tests/migration.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T07:56:00Z",
      "dryRunProof": {
        "treeSha": "db8f7fce66ba6076ae716187cfe1964c3632fc0f",
        "testsDiscovered": 10
      },
      "note": "P0-06 P-stage (2026-09-01): real, non-identity migration functions for the schema registry, proving the C-stage registry's migration-function slot works end-to-end. Two deliberately-synthetic, explicitly-labeled-as-illustrative examples (never wired into the real C-stage bootstrap): renameFiredAtToOccurredAt/renameOccurredAtToFiredAt (bidirectional/lossless rename) and mergeNameFields (irreversible/lossy merge, no reverse export). Both exercised via real registerSchema/evolveSchema calls against fresh synthetic schemaIds, never a parallel mechanism. packages/core/session/src/repair.ts deliberately left untouched -- pure crash-recovery with zero schema-version concept, on the far side of the registry's own leaf-object-only scope boundary (matching C-stage's identical known-event-types.ts precedent). Real RED at commit bea9541e91 (stub migrate.ts): 3/10 failed genuinely (wrong field names, no merge occurred), never module/syntax errors. Opus-level adversarial review (mandatory, L1_CONTRACT): 6 of 7 seeded mutations caught, including two proving the tests genuinely depend on real evolveSchema execution (bypassing the registry call, and corrupting evolveSchema's own migrate-storage) rather than testing the raw migration functions in isolation. One mutation (a renamed fake reverse-migration export evading the irreversibility guard's name-substring heuristic) survived -- noted as a non-blocking rigor gap, since the substantive information-loss test (independently verified by the reviewer via a pigeonhole counter-example proof, confirmed to generalize beyond the one shipped test input) already establishes the real acceptance-clause guarantee. Also fixed in this slice: a real, pre-existing verify-export-jsdoc escape in the already-merged P0-06.C src/index.ts (7 missing-doc violations the C-stage review missed), non-behavioral."
    },
    {
      "epic": "P0-06",
      "stage": "U",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "packages/sdk/server/tests/server.spec.ts",
        "packages/session/session-persistence-jsonl/tests/jsonl.spec.ts",
        "packages/settings/settings/tests/settings.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "rejects an initialize handshake whose explicit schemaVersion major differs from the registered major",
        "accepts an initialize handshake whose explicit schemaVersion major matches the registered major",
        "defaults an absent schemaVersion to the registered version, leaving ordinary handshakes unaffected",
        "rejects a session-event line whose explicit schemaVersion major differs from the registered major",
        "does not swallow a schema-incompatible line into the tolerant corrupt-suffix heuristic",
        "accepts a session-event line whose explicit schemaVersion major matches the registered major",
        "defaults an absent schemaVersion to this build's registered version, leaving ordinary replay unaffected",
        "does not negotiate schema for a packed chunk row tag (not a session-event payload)",
        "rejects registration when the stored section declares an incompatible major",
        "registers when the stored section declares a compatible major, stripping the tag before resolution",
        "keeps the last good value (no silent field loss) when a hot-reloaded section declares an incompatible major",
        "commits a hot-reloaded section that declares a compatible major",
        "defaults an absent $schemaVersion to the registered version, leaving ordinary reloads unaffected"
      ],
      "files": [
        "packages/sdk/server/tests/server.spec.ts",
        "packages/session/session-persistence-jsonl/tests/jsonl.spec.ts",
        "packages/settings/settings/tests/settings.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T15:35:00Z",
      "dryRunProof": {
        "treeSha": "98341625bc6049286e2a8d6fe84d81a88374861f",
        "testsDiscovered": 286
      },
      "note": "P0-06 U-stage (2026-09-01): real negotiateSchema() calls wired into 3 read boundaries -- session replay (session-persistence-jsonl/src/format.ts), SDK initialize (sdk/server/src/server.ts), settings load (settings/settings/src/index.ts) -- per BLOCKED-012's manifest-patch mechanism (2 files substituted for the wave-map's literal declaration: core/session/src/types.ts -> session-persistence-jsonl/src/format.ts, forced by a real ESM circular-dependency the Reviewer proved empirically; settings/settings/src/types.ts -> src/index.ts, since the types file is projection-only). Real RED->GREEN: the Writer reverted only the production wiring and confirmed genuine on-topic test failures (not module/syntax errors), then restored. A real design bug was found and self-fixed: negotiation must not fire for a type the registry never bootstrapped (that's the ignorable/legacy-type mechanism's jurisdiction) -- the first implementation wrongly treated any unregistered type as SCHEMA_UNKNOWN. Opus-level adversarial review (mandatory, L1_CONTRACT): ACCEPT with manifest-patch needed (both BLOCKED-012 conditions verified against spec/first100-owner-map.json, zero owner conflict) and 2 real gate violations found+fixed (verify-type-equiv drift in docs/subsystems/settings.md, a new oxlint no-confusing-void-expression violation), commit 7d8354e002. Real CI observation: run 33521991413 on the initial merge commit e05df35fec FAILED with a genuine deterministic regression (a typert-generated Cordis catalog artifact, packages/extensions/tool-cordis/src/api-catalog.ts, wasn't regenerated after this slice's SettingsRegisterOptions.schemaVersion? field addition) -- diagnosed and fixed (commit 12d0a1fdd1, also closing a separate pre-existing gap: ctx.trustKernel was never registered in the catalog generator's SERVICE_WALK_EXEMPTIONS, blocking regeneration entirely). Re-run: CI run 33524811126 on the fix commit 98341625bc completed with exactly 1 failure, packages/experimental/inspector's already-registered flake (5th documented occurrence on a distinct SHA) -- failure set fully absorbed by flake-registry.json per BLOCKED-007③, valid observation. This dry-run proof (treeSha 98341625bc, the exact CI-verified commit) confirms 286/286 tests passing locally, matching the CI observation's non-flake result. BLOCKED-013 recorded and folded into the standing gate-scan checklist as a result of this slice's CI-caught regression."
    },
    {
      "epic": "P0-06",
      "stage": "F",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "packages/session/session-persistence-jsonl/tests/jsonl.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "replays snapshots/web/fresh-round-trip/session.jsonl (a real pre-schema-registry session fixture) with no negotiation failure and no silent event loss",
        "replays snapshots/sdk/text-turn/session.jsonl (a real pre-schema-registry session fixture) with no negotiation failure and no silent event loss",
        "replays snapshots/session/skill-load/session.jsonl (a real pre-schema-registry session fixture) with no negotiation failure and no silent event loss"
      ],
      "files": [
        "packages/session/session-persistence-jsonl/tests/jsonl.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T16:15:00Z",
      "dryRunProof": {
        "treeSha": "e8240da70ea31d834c9d8d51e89e0c4e1fe8bb1c",
        "testsDiscovered": 161
      },
      "note": "P0-06 F-stage (2026-09-01), acceptance[0] (\"至少能够读取审计基线产生的旧 session fixture\"): proves U-stage's session-replay negotiation wiring doesn't break reading real OLD session data. Three real, pre-existing, already-committed fixtures from this repo's own keyless recorded-session snapshot corpus (snapshots/*/session.jsonl), all predating schema-registry's existence (no schemaVersion tag on any event). Real subtlety found and handled: committed fixtures are envelope-scrubbed (seq/time stripped by scrubSessionSnapshot for stable re-recording diffs), so reconstructRealSessionLog() rebuilds a real on-disk envelope using the identical seq-reassignment rule the codebase's own repackSessionSnapshot already applies in the reverse direction -- every event's real type/data payload stays byte-identical to the fixture, only seq/time are deterministically re-assigned. BLOCKED-012-class deviation from the wave-map's literal F-stage files (schema-registry/tests/compatibility.spec.ts, core/session/src/repair.ts): repair.ts confirmed out of scope and untouched (2 prior independent reviews plus this slice's own re-confirmation -- pure crash-recovery, zero schema-version concept, downstream of any negotiation); compatibility.spec.ts has zero visibility into the real wired boundary (SessionLogScanner.consumeEventLine's private negotiateEventLineSchema, reachable only via session-persistence-jsonl's own exports) -- extended jsonl.spec.ts's existing U-stage describe block instead, itself already P0-06's own frozen U-stage test file. Owner-map check (spec/first100-owner-map.json byFile, 441 entries): jsonl.spec.ts absent -- zero conflict. Coverage-completion slice, not a bug fix: all 3 cases pass cleanly against current U-stage wiring with no code changes. Real sensitivity proof: temporarily mutated format.ts's absent-schemaVersion default to an incompatible major, confirmed all 3 new tests genuinely fail with SchemaCompatibilityError/SCHEMA_MAJOR_MISMATCH, reverted (git diff on format.ts empty). Full session-persistence-jsonl + schema-registry suites: 280/280 passing."
    },
    {
      "epic": "P0-02",
      "stage": "F",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "packages/kernel/trust-kernel/tests/dispose.spec.ts",
        "packages/kernel/trust-kernel/tests/pin-hardening.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "leaves the kernel present after disposing an unrelated plugin, and shows the kernel is owned by the root fiber, not a plugin fiber",
        "rejects a plugin row applied to the root Include entry after boot exactly as it rejects one present at initial boot",
        "rejects delete-then-reprovide against a kernel pinned via pinTrustKernel, and leaves the original kernel pinned",
        "rejects a direct reassignment of the store entry (no delete) against a kernel pinned via pinTrustKernel, and leaves the original kernel pinned",
        "rejects `impl.value = forged` against a kernel pinned via pinTrustKernel, and leaves ctx.get('trustKernel') unforged",
        "rejects a direct write to ctx.root.fiber.store['trustKernel'], and leaves both ctx.get and ctx.trustKernel property access unforged for an unrelated sibling",
        "rejects a direct write to ctx.reflect.props['trustKernel'], and leaves ctx.trustKernel property access unforged",
        "durably poisons ctx.trustKernel property access for the attacking plugin itself, while ctx.get('trustKernel') and an unrelated top-level sibling stay correct -- this is the known, accepted gap, not a bug in pinTrustKernel",
        "poisons ctx.trustKernel property access for a sibling plugin mounted under the same ancestor, while ctx.get('trustKernel') and the root stay correct",
        "poisons ctx.trustKernel property access for every non-root context (siblings and later-mounted plugins alike), silently, while ctx.get('trustKernel') and the root's own direct property read stay correct"
      ],
      "files": [
        "packages/kernel/trust-kernel/tests/dispose.spec.ts",
        "packages/kernel/trust-kernel/tests/pin-hardening.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T19:30:00Z",
      "dryRunProof": {
        "treeSha": "c1abf813a834153d51814990ac7f9e516c2b3a68",
        "testsDiscovered": 10
      },
      "note": "P0-02 F-stage (2026-09-01), acceptance[0] (\"任意插件卸载、覆盖 service 或动态 mount 都不能替换 kernel policy/audit/signature verifier\"): closes BLOCKED-011, the multi-round Trust Kernel property-access security saga. BLOCKED-012-class deviation from the wave-map's literal F-stage file (boundary.spec.ts, already C-stage's own frozen, runtime-code-free type-surface test -- structurally cannot host a runtime unload/mount/bypass proof); manifest-patched to dispose.spec.ts + pin-hardening.spec.ts, recorded in tests/first100/adjudication.json#deliverablePathPatches (P0-02-F-dispose, P0-02-F-pin-hardening). dispose.spec.ts (commit e7d49cfa6b) proves the 'unload' half (a real Loader-driven fiber disposal, the root-pinned kernel survives and is genuinely owned by the root fiber, per ctx.reflect.store/Context.isolate) and the 'dynamic mount' half (Entry.update() on the root Include entry's config.patches, the one real post-boot insertion path this codebase supports, reduces to the same ctx.provide()-double-call rejection U-stage already proved) plus 2 direct-store-mutation bypass tests (delete-then-reprovide, direct reassignment) added during BLOCKED-011's hardening rounds. pin-hardening.spec.ts (commit bd993fa6fa and predecessors) proves the 'override' half across 4 real forgery rounds -- Object.freeze(impl) closing in-place Impl mutation, a root-fiber-store key lock, a reflect.props key lock -- plus 3 documented residual vectors (self-fiber, ancestor-fiber/vector G, wholesale root-store replacement/vector H), each independently reproduced live and proven currently unreachable by any real consumer (exhaustive grep: zero real code anywhere reads ctx.trustKernel via property access; the one real consumer, apps/cli/src/profile-boot.ts:314, uses ctx.get exclusively). Went through 4 real REJECT rounds plus a pre-committed HARD STOP before the user's final BLOCKED-011 decision (spec/first100/exec/BLOCKED-QUEUE.md#BLOCKED-011, ANSWERED-BY-USER final, 2026-09-01): P0-02.F closes at this honestly-documented state -- three property-access branches (dotted/bracket-literal/destructuring) closed to pure lexical matching with a narrow explicit allowlist, `scripts/verify-trust-kernel-property-access.ts` wired into the CI gate set -- with a new standing hard-trigger rule requiring the vendored Cordis Fiber structural fix (Option A) before any future epic wires a real policy/audit/signature-verifier enforcement point (~W6). All 6 known-limitation doc surfaces (EN/ZH boundary doc, EN/ZH package README, src/index.ts JSDoc, AGENTS.md) carry the deferred-not-permanently-accepted annotation. Coverage-completion dry run: 10/10 passing at the exact commit merging this decision (c1abf813a8, which also merge-forwards first100-exec's independent P0-06 ACCEPTED work into this branch)."
    },
    {
      "epic": "P0-03",
      "stage": "C",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "tests/architecture/capability-seams.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "accepts a well-formed family with a definition, providers, and consumers",
        "rejects a family with an empty id",
        "rejects a family with a duplicate provider entry",
        "rejects a family with a duplicate consumer entry",
        "rejects two families sharing the same id",
        "rejects a family whose definition, provider, or consumer package is absent from the workspace",
        "derives one allowed edge from each provider to the family definition",
        "derives one allowed edge from each consumer to the family definition",
        "derives no edge between two providers or between two consumers",
        "requires every allowlist entry to carry a removalDate in YYYY-MM-DD form",
        "rejects an allowlist entry whose removalDate is not a real calendar date",
        "requires every allowlist entry to carry a non-empty owner",
        "rejects a duplicate allowlist entry for the same kind, from, and to",
        "parses and validates every declared family with zero schema violations",
        "gives every allowlist entry a removalDate and an owner",
        "reports zero missing-provider violations under the controlled allowlist (acceptance[0])",
        "flags a consumer import resolving into a provider package's src/* as a deep-import violation",
        "does not flag a consumer import resolving to a provider package's published entry point",
        "does not flag an import between packages outside any declared family",
        "flags a provider import resolving into an app/UI package as a provider-app-dependency violation",
        "does not flag a provider import resolving to its own family definition package",
        "flags a family with zero providers and no matching allowlist entry",
        "does not flag a family whose only provider is its own definition package",
        "flags a family missing a provider fixture, consumer composition test, or unload/rollback test",
        "does not flag a family whose test evidence covers all four must[2] artifacts",
        "names the offending dependency edge, source file, and a remediation suggestion on every violation kind",
        "suppresses a violation whose kind and edge match an allowlist entry",
        "still reports a violation whose edge is not covered by any allowlist entry"
      ],
      "files": [
        "architecture.layers.json",
        "scripts/architecture/capability-seams.ts",
        "tests/architecture/capability-seams.spec.ts",
        "packages/README.md"
      ],
      "frozenAtUtc": "2026-09-01T21:15:00Z",
      "dryRunProof": {
        "treeSha": "259f1e9eb244c1d4ed0c8f7d03091313ab3ad26b",
        "testsDiscovered": 28
      },
      "note": "P0-03 C-stage (2026-09-01), the Capability Seam architecture-consistency checker's Contract type surface: per-family declarations (definition/providers/consumers/allowed-dependency-edges, must[0]), an allowlist schema requiring a removal date and owner per entry (acceptance[1]), and pure detector functions for all three acceptance[2] violation kinds (deep import, missing provider, non-reversible registration) plus must[1]'s second rule (provider-app reverse dependency) -- all taking already-loaded data as parameters, zero filesystem I/O, deferring the real workspace scan to a future U-stage `.mjs` scanner per the registry's own C/U split. Real RED->GREEN: a stub returning empty/false unconditionally failed 18/27 genuinely (real AssertionErrors, never module/syntax errors); full implementation passed 27/27, later 28/28 after a fix-round addition. `architecture.layers.json`'s 29 real capability families were transcribed from `scripts/gen-doc-graphs.ts`'s already-curated SERVICE_ROLES seam entries (this repo's existing authoritative source, already used to generate docs/capability-seams.md). Round 1 independent review REJECTed on 4 real findings: (1) an undeclared C-stage file, `scripts/architecture/capability-seams.ts`, added with zero BLOCKED-012 manifest-patch; (2) that file contained a real `globSync`+`readFileSync` workspace scan -- genuine C/U-stage scope creep, since the registry reserves the real scanner for a separate U-stage `.mjs` file; (3) acceptance[0] ('existing repo passes under the allowlist') was only partially tested, missing a check of `detectMissingProviderViolations` against the real loaded data; (4) several families' `consumers[]` arrays undercounted real workspace dependency edges. Fix round: removed all filesystem I/O from the library file (moved the workspace scan into the spec file's own test-local helper), filed a real BLOCKED-012 manifest-patch (`P0-03-C-detection-logic` in tests/first100/adjudication.json#deliverablePathPatches, count 6->7), added the missing acceptance[0] test (empty-result assertion against real data, non-vacuously verified by an independent reviewer who temporarily zeroed a real family's providers and confirmed genuine failure), and reconciled all 29 families' consumers[] against real package.json dependency edges via a full sweep (not sampling). Round 2 independent review: ACCEPT, with every fix independently re-verified (fs-freedom confirmed by grep, manifest-patch format matched against the 6 existing precedents, acceptance[0] test proven non-vacuous via a live mutation, and a full independent 29-family consumer-edge sweep finding zero missing edges) -- one minor, non-blocking note left for a future U-stage slice: 2 stray consumer entries (dsh-bash-sandbox on subprocess, dsh-tool-bash on sessionPersistence) reflect devDependency-only test wiring rather than a real production/peer dependency, functionally inert since no consumer of this data exists yet."
    },
    {
      "epic": "P0-07",
      "stage": "C",
      "argv": [
        "pnpm",
        "exec",
        "vitest",
        "run",
        "tests/release/evidence-package.spec.ts",
        "--reporter=json"
      ],
      "expectExit": 0,
      "expectCases": [
        "imports only a type-only Branded from @deepseek-ai/dsh-brand -- no other import, no runtime dependency edge",
        "exports no Config schema and no apply(ctx, config) plugin entry -- nothing here is a Cordis plugin export",
        "declares GateEvidence as a discriminated union of CompletedGateEvidence | SkippedGateEvidence | MissingGateEvidence",
        "gives GateEvidenceBase (unexported) exactly the must[0] base fields, all readonly",
        "exports CompletedGateEvidence with exactly its own declared members, all readonly, extending GateEvidenceBase",
        "exports SkippedGateEvidence with exactly its own declared members, all readonly, extending GateEvidenceBase",
        "exports MissingGateEvidence with exactly its own declared members, all readonly, extending GateEvidenceBase",
        "types Digest as Branded<EvidenceDigest\"> -- a plain nominal string, never a unique-symbol handle",
        "types Signature as Branded<EvidenceSignature\"> -- a plain nominal string, never a unique-symbol handle",
        "types CommitSha as Branded<CommitSha\"> -- a plain nominal string, never a unique-symbol handle",
        "types GateId as Branded<GateId\"> -- a plain nominal string, never a unique-symbol handle",
        "type-checks well-formed CompletedGateEvidence, SkippedGateEvidence, and MissingGateEvidence literals with zero diagnostics",
        "rejects a CompletedGateEvidence literal whose skipReasons is nonempty -- a completed gate carries no skip reason",
        "rejects a SkippedGateEvidence literal whose skipReasons is empty -- a skip must name at least one real reason",
        "rejects a MissingGateEvidence literal whose skipReasons is empty -- a missing gate must also name at least one real reason",
        "rejects reassigning a GateEvidence member -- every field is readonly (TS2540)",
        "rejects a plain string literal where a Digest is required -- Digest is a Branded nominal type, not a bare string",
        "binds baselineFingerprint, gitDiff, additionalGates, and signature on EVERY EvidencePackage, before any accepted-narrowing",
        "types BaselineFingerprintBinding.gitSha, GitDiffBinding.baseSha, and GitDiffBinding.headSha as the CommitSha brand -- distinct from GateId, not a bare string that would satisfy any brand",
        "type-checks an AcceptedEvidencePackage literal when every required gate id is present and CompletedGateEvidence, and every required artifact path is present",
        "rejects an AcceptedEvidencePackage literal that omits one required gate id from requiredGates",
        "rejects an AcceptedEvidencePackage literal whose requiredGates assigns a SkippedGateEvidence to a required gate id",
        "rejects an AcceptedEvidencePackage literal whose requiredGates assigns a MissingGateEvidence to a required gate id",
        "rejects an AcceptedEvidencePackage literal that omits one required build artifact path from requiredBuildArtifacts -- must[2]'s \"missing artifact\" clause",
        "type-checks an UnacceptedEvidencePackage literal whose requiredGates has a SkippedGateEvidence and whose requiredBuildArtifacts is empty -- accepted:false imposes no completeness or outcome constraint",
        "documents the honest degraded case: with RequiredGateId/RequiredArtifactPath left at their string default, an AcceptedEvidencePackage literal with EMPTY requiredGates and requiredBuildArtifacts maps still type-checks -- completeness of a non-literal required-id SET is a P-stage runtime check (verify-evidence.mjs against the release manifest), never a static one this type surface can express",
        "declares no unique-symbol-branded member anywhere in the module -- every field is a plain JSON-safe value, so JSON.stringify never silently drops it",
        "is exactly one statement: `export type * from './types.ts'` -- zero runtime exports, zero Cordis registration, zero side effects",
        "has no default export and no Config/apply plugin exports"
      ],
      "files": [
        "packages/assurance/evidence-format/src/types.ts",
        "tests/release/evidence-package.spec.ts"
      ],
      "frozenAtUtc": "2026-09-01T21:20:00Z",
      "dryRunProof": {
        "treeSha": "859a2d017e04aec1cf25aacf22d7003107613fdb",
        "testsDiscovered": 29
      },
      "note": "P0-07 C-stage (2026-09-01), Release Evidence Package's Contract type surface: a discriminated GateEvidence union (CompletedGateEvidence | SkippedGateEvidence | MissingGateEvidence, must[0]) carrying command/timestamps/exitCode/environment/digests/testCounts/skipReasons; an EvidencePackage binding baselineFingerprint + gitDiff + build-artifact digests (must[1]); and the epic's central claim, must[2] ('any skipped blocking gate or missing artifact must never allow accepted=true') proven as a real compile-time guarantee: AcceptedEvidencePackage<RequiredGateId, RequiredArtifactPath> requires complete Record<K, CompletedGateEvidence>/Record<K, Digest> maps keyed by literal-union required-id sets, independently verified via direct tsc diagnostics (TS2741 on a missing key, TS2322 on a skipped/missing gate where completed is required) -- not merely asserted by a test, a real compiler rejection. One honest, disclosed limit: with RequiredGateId/RequiredArtifactPath left at their string default, an EMPTY requiredGates/requiredBuildArtifacts map still type-checks -- required-id-SET completeness is P-stage's runtime job (verify-evidence.mjs against the release manifest), not something this type surface can express statically; a dedicated test documents this rather than hiding it. Confirmed the registry's third declared C-stage file, spec/first100-evidence.schema.json, is a coincidental path collision (this program's own 109-epic self-tracking schema, unrelated id/lane/baselineSha vocabulary) not a real dependency -- independently re-verified by the Reviewer, not just the Writer's own claim. Real RED->GREEN: a deliberately wrong stub types.ts failed 25/29 genuinely (TS2305 missing-export errors, AST shape mismatches, zero crashes); the real implementation passed 29/29. Round 1 independent review: ACCEPT on design/tests/central claim, REJECT on one purely mechanical miss -- gen-module-graph.ts --check failed because the new packages/assurance/evidence-format package was never reflected in the generated module-graph docs. Fix round: ran gen-module-graph, committed the 3 regenerated docs, touching nothing else. Round 2 independent review: ACCEPT, with the compile-time must[2] guarantee re-proven from scratch via a standalone tsc probe (not reusing the test file) and the fix commit's diff confirmed scoped to exactly the 3 doc files."
    }
  ]
}
