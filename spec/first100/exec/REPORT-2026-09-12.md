# First-100 · 2026-09-12 日报(delegate first100-delegate-78)——截至 2026-09-13 04:05 EDT

## 一、结果
- **两笔推送到 `fork/first100-exec`**:⑤ `2b54e42991`(08:50 EDT,候选 4′)、⑥ `d10920efa4`(11:36 EDT,候选 5′)。远端现在 = `d10920efa4`。
- **ACCEPTED 仍 26**;BLOCKED_ON_ACCEPTANCE 3(P2-01 待你放行、P5-10、P4-12);NOT_RUN 72。新绿格:P2-12 C/P/U、P3-01 C/P/U/F(**四格全绿,签核复核中**)、P4-02.U.2(出厂 base 行到达的 composition 观测)。
- 候选 4/5 各红一次再绿:候选 4 红两处确定性(新 subsystem 页缺索引行、两个新挂载包缺 SDK runtime 闭包依赖),候选 5 红三处全出自 lane B 一笔"lint 修补"(JSDoc 缺标签、cordis catalog 没重生成、裸 vendor 令牌)+ 本机复现的 usage-subject 门。**每一次红都定因、修、记规则;没有一次靠重跑过关。**
- 今日落地的产品/证据(摘要):P3-01 C/P/U/F 全阶段(ExecutionWorld 契约、local provider、派发路径绑 world + policy 事实第二变体、故障矩阵 + conformance);P2-12 C/P/U(紧急停止通道:控制面、持久 stop、闸门、SDK 首条 server→client 请求、ask 交缝);P2-06 C/P/U 写完(审批绑定:提问时刻捕获、`approval/bound` 会话事件、执行前重验拒绝派发、六字段抵达 ACP/Web 两真实答复者)——U 待冻;BLOCKED-220 全修(丁 记录被放弃的 job + `job/abandoned` 会话事件 + 甲 有界 drain,候选 6);BLOCKED-229/230(run-store 写逃出 dispose;测试丢弃 Context)修 + 共享运行时守卫;BLOCKED-231/232/233;P2-10 preFlight + C 表;P4-02 签核材料(不签,见二)。

## 二、签核与撤签
- **P3-01**:四格 GREEN;两条冻结时声明的开放项(跨真实 provider 的等价半边——只有 local 与 fake;attestation 验证器仍是 P0-02 的惰性槽)。coverage 引用与 4.4 材料 lane B 补中,补齐后我按"已声明限制"形状签或不签。
- **P4-02 不签(BLOCKED-232)**:三条 clause 生产读者为零(`Agent.taskProfile` 一写零读、questions 无提问者、`validateTaskProfile` 零调用)——与 P5-10/P2-01 同形。归属表:读者按 registry 归 P2-12 / P4-03 / P5-01,写成 DEFERRED-TO;P4-02 自欠的读回校验已落(`run/run:138` fail-closed)。
- **P2-01 待你放行**(见三·0)。

## 三、需要你定的
0. **P2-01 重新放行**(阻塞 ACCEPTED 26 → 27):你 09-02 的确认 C9 对应撤签前;现四格 GREEN、4.4a–d 材料齐、tenant 缺口(228)已修。工具要求新的 `decisions-approved` 引用,我不复用也不伪造。**推荐:一句"P2-01 放行"。**
1. **子代理能否经父 surface 向人提问(B1)**:今天被 `DELEGATED_CALLER` 刻意拒绝,P5-10 acceptance[1] 主语因此不可达。推荐允许"父代问",冻结后独立切片;默认不做。
2. **已声明但无执行者的纪律**通则(到期日/义务转移/P0-06 registry 覆盖所有持久对象但生产注册者仅 settings 一个):推荐"新条目须指出执行者或明写无执行者、旧条目不回溯";默认按推荐。
3. **无 lease 组合的外部副作用**:action-ledger 对无 lease 的 agent 只承诺 at-least-once(显式 unfenced 进审计);要不要 fail-closed 是产品决定;推荐先 fail-open + 审计,等 P4-07 让每个 agent 有 lease。
4. 附注(已裁、可推翻):P2-10 "一万条历史 fixture" 重述为真实语料重放 + 一万条派生演练;"有限声明式语言" = dsh 的 Cedar schema + 约定,不造第二语法。文档预算余量、`packages/README` 清单表豁免:非阻塞。

## 四、过程规则(今日进 ONBOARDING)
- 报 tip 前九项:三组只在全套里活的门、三生成物门、hygiene、处置与 C 冻结同笔、registry 全组逐门——两条 lane 今天各因漏跑红过候选。
- 证明脚本先过反向控制;控制/变异先证明改变了被测条件(四例);顺序断言不证明值何时被读;长跑日志两端记 HEAD 与时刻(跨 rebase 或内存崩塌窗口的读数作废);`git rebase --onto` 的 base 被排除;切分支后 lib 旧、重录前 build;refresh 的 `-t` 不限制写盘;共享机器只杀自己的 PID;`DELEGATE-ONBOARDING.md` 归 delegate 写。
- 我自己的错今天记了六处(零差异前提、"四笔"数错、P2-10 preFlight 存在与否、U.2 预检脚本误报、两处"复用/老先例"前提),每处都在补记里点名。

## 五、机器
- 宿主内存长期 60–100 MB free(外部 Chrome/Codex),本机 build 与全仓 `vitest list` 反复被杀;lane 改为分步 build、分包收集、逐门落盘;云跑仍是唯一全量观测。

## 六、行为变化
- **紧急停止**(候选 4 起出厂):`ctx.controlPlane` 挂在 `dsh-base`,stop 持久化、重启后保持、须显式解除;SDK 协议首条 server→client 请求 `human/question`(出厂无 handler → 拒绝)。
- **执行世界**:`workspace-write` 下每次派发绑一个 world 并记 `action/world-bound`(id 与 spec 摘要在语料里占位符化);`full-access` 下 local provider 诚实拒绝、不绑。
- **一次性 run 的在飞 job**:候选 5 起退出前记 `job/abandoned`(含 stderr 行);候选 6 起按 config 上界(默认 30 s)等待结算再退出,不取消。
- **审批绑定**(候选 6 起):提问时刻捕获参数摘要/principal/manifest 等,执行前重验,任一字段变化即拒绝派发。
