{
  "schema": {
    "name": "first100-files-overlay-reasons",
    "version": "1.0"
  },
  "note": "The human half of \u00a712.4's overlay: why a source path the plan never named is nevertheless inside its epic's scope. Keyed `<epic> <path>`. Entries marked HOT ZONE touch a file \u00a72.D lists as shared, and the delegate reads those individually rather than accepting the sentence.",
  "reasons": {
    "P0-03 scripts/architecture/capability-seams.ts": "The seam-classification rules themselves. The registry declared the checker but not the module holding the rules it applies, and a frozen case naming a classification has to name where that classification is decided.",
    "P0-03 scripts/architecture/check-capability-seams.d.mts": "The declaration file for the .mjs checker. Repo convention pairs every scripts/*.mjs with a .d.mts so TypeScript consumers see its API; the registry named only the runtime half.",
    "P0-04 scripts/architecture/layer-order.ts": "The canonical layer sequence and edge-classification rules must[0] is about. The registry declared the scanner (a later stage) and not the contract module the scanner applies.",
    "P0-08 benchmarks/harness-capability/scenarios/index.ts": "The scenario table the fault-matrix cases enumerate. A benchmark scenario list is neither test nor product source, and the registry file list has no benchmarks/ entry at all.",
    "P1-01 packages/plugin/plugin-manifest/src/invariant.ts": "The published ./invariant companion. Package rules require the companion to live beside the manifest module it observes; the registry declared the manifest module alone.",
    "P1-02 packages/host/plugin-inventory/src/index.ts": "The consumer that reads recorded provenance in production. must[2] is about verification reaching a real inventory, and the inventory is where that call site lives.",
    "P1-02 packages/kernel/trust-kernel/src/index.ts": "HOT ZONE. Written from `git show 05c22146ef -- packages/kernel/trust-kernel/src/index.ts` (+42/-1): `createTrustKernel` takes a new `config: TrustKernelConfig` carrying `trustAnchors`, a module-private `CONFIGURED_ANCHORS` WeakMap keyed by the handle holds them, and `configuredTrustAnchors` is a new export. That is a change to construction, not a read of it. The WeakMap keying is the load-bearing part: only a caller already holding the unforgeable handle can read the anchors, and a forged object reaches nothing because it was never a key. `createTrustKernel` still performs no I/O \u2014 the boot path reads configuration and passes the result \u2014 so the constructor stays total. An earlier version of this entry said P1-02 only READS `signatureRoots` and does not change construction, which was simply false.",
    "P1-03 packages/plugin/plugin-lock/src/candidate.ts": "Candidate-lock construction, which is half of must[0]: a lock that pins integrity has to be built somewhere, and the registry declared only the comparison side.",
    "P1-03 packages/plugin/plugin-lock/src/commit.ts": "The atomic commit path. must[0] requires the lock to be written durably; the registry named the decision functions and not the write.",
    "P1-03 packages/plugin/plugin-lock/src/gate.ts": "The boot-admission decision the U stage proves. The registry declared the lock package broadly and this file is where admission is decided.",
    "P1-03 packages/plugin/plugin-lock/src/lockfile-integrity.ts": "Created by this epic to close BLOCKED-135: real integrity read from pnpm-lock.yaml, replacing a `dsh.provenance.integrity` field nothing wrote. The plan predates the finding, so it could not have named the file.",
    "P1-03 scripts/first100/verify-boot-path-offline.mjs": "The structural gate \u00a712.1 substituted for the withdrawn fetch-spy assertion: the boot closure contains no HTTP client. It is program infrastructure the epic was told to build, not product code.",
    "P1-07 packages/skill/skill-filesystem/src/index.ts": "The provider whose filesystem access the trust decision gates. The clause is about what a skill may reach, and this is the reaching.",
    "P1-07 packages/workspace/workspace-trust-local/src/index.ts": "The local trust store the decision reads. The registry declared the policy and not the store it consults.",
    "P1-08 apps/cli/src/profile-boot.ts": "HOT ZONE. Written from `git show c3992e5124 -- apps/cli/src/profile-boot.ts` (+52/-3): profile boot imports `negotiateProfileLayerCompatibility` and `DSH_RUNTIME_API_VERSION`, runs the negotiation on the admitted layers before any patch reaches `boot()`, and the result gains a `compatBlockedLayers` field whose layers are absent from `bundlePatches` \u2014 so a blocked layer's plugin code never mounts (acceptance[1]). The change is an added step in the boot sequence, not a change to what boot already did. It is in this epic's scope because the clause is about boot ADMISSION: proving it in the solver alone would leave the refusal unreached by any launch.",
    "P1-09 packages/extensions/cordis-host-runner/src/guard.ts": "The runtime guard that attributes a tool call to its owning plugin. Ownership is decided here, and the registry named only the attribution rules.",
    "P1-09 packages/extensions/cordis-host-runner/src/index.ts": "The host runner entry that installs the guard above. Its registration is what puts attribution on the live path.",
    "P1-09 packages/extensions/tool-cordis/src/api-catalog.ts": "The catalog surface whose entries carry owner attribution to the model.",
    "P1-09 scripts/gen-cordis-catalog.ts": "The generator that produces the catalog the case reads. A generated artifact cannot be frozen without naming its generator.",
    "P2-01 packages/identity/principal/src/invariant.ts": "The published ./invariant companion for the principal package, required beside the module it observes.",
    "P2-02 packages/kernel/trust-kernel/src/index.ts": "HOT ZONE. Written from `git show 1ac2dfe4d1 -- packages/kernel/trust-kernel/src/index.ts` (+31/-5): the module imports `generateKeyPairSync`/`sign`/`verify` from node:crypto, mints an Ed25519 keypair per kernel, and holds it in a module-private WeakMap keyed by the handle \u2014 so the PRIVATE key is never a property of `signatureRoots`, which crosses the plugin boundary. `signWithSignatureRoots`/`verifyWithSignatureRoots` are new exports, and a handle this module did not mint is refused rather than handed an empty signature. This replaced a four-byte constant that was identical in every installation, so any deployment's token verified anywhere and verification could not fail (P2-02 must[1]). Construction changed here under P2-02's acceptance lock, and the lift records the same facts.",
    "P2-03 packages/core/session/src/index.ts": "HOT ZONE. Written from `git show ed09684ecf -- packages/core/session/src/index.ts` (+24/-0): the Session log gains a private `typeCounts` Map, maintained on every append, and a public `countEventsOfType(type)` reading it. Why: must[1] needs the manifest `sequence` to be one running position across two call sites (the native tool path and the PTC code-mode sub-dispatch), and a per-append counter avoids the O(n) scan a hot path would pay per operation. The method is deliberately generic \u2014 this Session names no manifest \u2014 because the event map is merge-extended downstream and a manifest-specific counter here would invert that dependency. An earlier version of this entry said the epic \"contributes an event type and does not change the log itself\", which described the declaration merging in dsh-action-manifest and hid this file's actual change.",
    "P4-06 packages/collaboration/intake-dedup/src/dedup.ts": "Created by this epic under ruling \u00a712.5 B: the single (id, epoch) rule both consumers now import, replacing two implementations (BLOCKED-136). The plan predates the finding.",
    "P4-06 packages/collaboration/intake-dedup/src/index.ts": "The new package barrel, forced by the repository convention that every packages/*/* directory carries an index.ts entry (B4(f) scaffold).",
    "P4-06 packages/collaboration/mailbox/src/index.ts": "P5-11 OWNS THIS FILE. Changed here so its second copy of the dedup rule becomes an import of the shared one; the mailbox keeps its own address check and its exported names, and P5-11's four live frozen cases pass unchanged in the same observation (BLOCKED-059 precedent).",
    "P4-06 packages/run/message-bus/src/bus-store.ts": "The durable store must[0] names: one BEGIN IMMEDIATE over the domain event, its outbox rows and the inbox claim. Created by this epic after the clause was reworded (C13) to exclude the KV seam.",
    "P4-06 packages/run/message-bus/src/dispatcher.ts": "The dispatch loop that turns outbox rows into delivery attempts, which is the at-least-once half of the clause.",
    "P4-06 packages/run/message-bus/src/mailbox-delivery.ts": "must[2]'s production call site: a real mailbox arrival decided against the durable consumed set. It lives in this package rather than in dsh-mailbox because the store is orchestration-runtime and the mailbox is a capability definition (ruling \u00a712.5 B).",
    "P4-07 packages/core/agent/src/index.ts": "HOT ZONE. Written from `git show c79a24f78c -- packages/core/agent/src/index.ts`: the barrel had ZERO occurrences of `advanceAgentLifecycle` before this commit, and P4-07 added BOTH `advanceAgentLifecycle` and `advanceAgentLifecycleFenced` to it. So the unfenced public export is this epic's own widening, not a pre-existing condition it inherited \u2014 P4-07's standing open finding is one it created. Measured on the same question: `src/dispatch.ts` already exported the function and `package.json` already carried `\"./src/*\"`, so `@deepseek-ai/dsh-agent/src/dispatch` was importable beforehand; what this commit added is reachability from the package root, which is the surface the finding is about. An earlier version said the unfenced export was \"still\" exported, which read the epic's intent instead of its diff.",
    "P4-08 packages/workflow/workflow-journal/src/observer.ts": "The observer that turns journal records into an observable stream, which the U stage's clause is about.",
    "P4-08 packages/workflow/workflow-journal/src/recorder.ts": "The recorder that writes journal entries. The registry named the journal package without naming where a record is produced.",
    "P4-09 packages/workflow/workflow-registry/src/nesting.ts": "Nesting-depth decisions, which is the subject of the U-stage clause.",
    "P4-09 packages/workflow/workflow-registry/src/store.ts": "The registry store the P-stage clause persists through.",
    "P5-10 packages/subagent/subagent/src/control-convergence.ts": "Convergence decisions for a delegated run, created by this epic as the C-stage contract module.",
    "P5-10 packages/subagent/subagent/src/control-router.ts": "The router that applies those decisions, created by this epic as the Provider half.",
    "P5-10 packages/subagent/subagent/src/control-types.ts": "The types both halves share; separated because src/types.ts contains only types by repository rule.",
    "P5-10 packages/subagent/subagent/src/index.ts": "The package barrel, which must re-export the control surface for any consumer to reach it.",
    "P6-01 packages/context/memory-context/src/index.ts": "The context contributor whose recall query the clause constrains.",
    "P6-02 packages/memory/memory/src/index.ts": "The memory service the clause is about; the registry declared the package and not its entry point.",
    "P6-07 packages/session-query/session-query/src/corpus.ts": "The corpus assembly the search clause queries over.",
    "P6-07 packages/session-query/session-query/src/types.ts": "The query and result types the corpus and its callers share.",
    "P6-07 packages/session/session-lifecycle/src/store.ts": "The lifecycle store the P-stage clause persists through.",
    "P8-01 scripts/gen-control-protocol-schema.ts": "The generator for the control-protocol schema the frozen case compares against. A generated artifact cannot be frozen without naming its generator.",
    "P2-03 packages/action/action-manifest/src/identity.ts": "Created by this epic under \u00a712.11: the actor resolution and the idempotency-key derivation both execution paths need. Not a hot zone \u2014 a new file in the package that already owns the manifest \u2014 but recorded because the plan named neither. It exists rather than living in each caller for the reason BLOCKED-136 measures: two paths deriving the same key is one rule with two implementations, and the derivation IS the contract (a replayed attempt keys the same, a new one does not).",
    "P4-07 packages/workflow/workflow-worker-thread/src/run-lease.ts": "Created by this epic in the U rebuild (\u00a712.15-2): the run-side view of a lease \u2014 acquire, renew, and may-I-write. It exists rather than living inline in `host.ts` so `mayWrite` reads through the lease package\u2019s own `checkFencing` instead of comparing epochs again; a second implementation of \"is this token current\" would be BLOCKED-136\u2019s shape on the check that decides whether a stale worker may write.",
    "P4-07 packages/workflow/workflow/src/index.ts": "Two additions to `WorkflowErrorCode`: `LEASE_STORE_UNAVAILABLE` and `RUN_HELD_BY_ANOTHER_HOST`. No existing member changed. They belong to the seam DEFINITION rather than to the worker-thread provider because the code union is the seam vocabulary \u2014 a provider inventing its own codes would make the seam callers switch on strings the definition does not know \u2014 and acceptance[2] needs a caller to tell a store outage from an ordinary refusal, which an untyped throw cannot carry. NOT labelled HOT ZONE: an earlier draft was, and the citation gate refused it for naming `HEAD` rather than a sha. Checking rather than arguing with my own gate: this file is not on \u00a72.D hot-zone list (P3-01, P3-05, P3-07, P5-05, P5-06), so the label was over-classification, and a label applied outside its defined set makes the five that carry it mean less.",
    "P4-07 packages/workflow/workflow-worker-thread/src/index.ts": "The engine plugin, where the lease is ACQUIRED \u2014 before the worker thread exists. Acquiring after the thread starts would leave a window in which two hosts both run the script, and no later fencing check undoes it: a refused write does not un-send what the worker already did. The heartbeat and the write guard deliberately do NOT live here; gate (u) refused an earlier version that put them in the plugin, and it was right on the design too, since the lease lifetime is the run lifetime. `leaseMs`/`heartbeatMs` are Config fields added in the same change.",
    "P4-08 packages/workflow/workflow-journal/src/index.ts": "The package moved to `packages/collaboration/workflow-journal` under \u00a712.22-1 (BLOCKED-149) and the registry's declared paths moved with it. This entry's observation is EARLIER than the move and cites the path as it existed when the cell was greened; rewriting it would restate what a past CI run saw, which is provenance rather than a file list. The path is therefore outside the current declaration by history, not by scope.",
    "P4-08 packages/workflow/workflow-journal/src/replay.ts": "The package moved to `packages/collaboration/workflow-journal` under \u00a712.22-1 (BLOCKED-149) and the registry's declared paths moved with it. This entry's observation is EARLIER than the move and cites the path as it existed when the cell was greened; rewriting it would restate what a past CI run saw, which is provenance rather than a file list. The path is therefore outside the current declaration by history, not by scope.",
    "P4-08 packages/workflow/workflow-journal/src/types.ts": "The package moved to `packages/collaboration/workflow-journal` under \u00a712.22-1 (BLOCKED-149) and the registry's declared paths moved with it. This entry's observation is EARLIER than the move and cites the path as it existed when the cell was greened; rewriting it would restate what a past CI run saw, which is provenance rather than a file list. The path is therefore outside the current declaration by history, not by scope.",
    "P4-06 packages/core/agent/src/index.ts": "The barrel that publishes `DuplicateArrivalError`, the refusal a caller must be able to catch. NOT labelled HOT ZONE: it gains two export lines and no behaviour.",
    "P4-06 packages/subagent/subagent/src/continuation.ts": "HOT ZONE. From `git show fee9034530 -- packages/subagent/subagent/src/continuation.ts`: `SubagentSettledMessageSource` gains an optional `senderEpoch`, and the settlement notice reads it from the child's `Agent.lifecycle` before the handle is disposed. The sender has to state its generation or must[2]'s triple has only two thirds of an identity; this epic's consumer is the parent's inbox, and the epoch is the one thing the inbox cannot derive.",
    "P4-08 packages/collaboration/workflow-journal/src/store.ts": "The journal's persistence, which must[1] requires and which the package did not have: every decision in it is about a run that already died, so `planResume` had no possible input while the journal lived only in memory.",
    "P4-08 packages/workflow/workflow-worker-thread/src/index.ts": "HOT ZONE. From `git show 07acd35c6e -- packages/workflow/workflow-worker-thread/src/index.ts`: `start` and the new async `resume` both delegate to one private `launch`, the journal directory is derived from the storage root, and `childFinished` reads the child's DURABLE session. Shared with P4-07, whose lease acquisition is in the same `launch` body and is unchanged by this.",
    "P4-08 packages/workflow/workflow-worker-thread/src/resume.ts": "The reconciliation itself: which recorded steps a resumed run may reuse, and the script digest `admitResume` compares. New file; the plan named the journal package but not the engine-side half that reads it back.",
    "P4-08 packages/workflow/workflow-worker-thread/src/types.ts": "`WorkerInit` gains `reusable`, the only channel by which a reconciled decision reaches the worker: `workerData` is structured-cloned at spawn, so anything the script's `agent()` consults has to be there before the thread exists.",
    "P4-08 packages/workflow/workflow/src/index.ts": "HOT ZONE. From `git show 07acd35c6e -- packages/workflow/workflow/src/index.ts`: `WorkflowEngine` gains the abstract `resume(runId, request): Promise<WorkflowRun>`. A Service Definition change, so every implementation must answer it \u2014 three test stubs did. \u00a712.23 chose this over making `start()` async for callers that have no run to resume.",
    "P4-08 packages/collaboration/workflow-journal/src/recorder.ts": "The recorder the P-stage freeze cites, at its path after \u00a712.22-1 moved the package. Carried into the overlay by the superseding entry rather than by new work: the file's content is unchanged, only its directory is.",
    "P4-09 packages/workflow/workflow-worker-thread/src/host.ts": "HOT ZONE. From `git show d3a23a5e2c -- packages/workflow/workflow-worker-thread/src/host.ts`: `WorkerRun` gains `onNestedStart`/`nest`, a `nestedRuns` set, and a `NestingPort` constructor argument; `cancel()` gains the `cancelPropagationForNested` block. Shared with P4-07 (the lease) and P4-08 (the journal), whose code in this file is untouched by the diff \u2014 the nesting additions sit beside them and change neither.",
    "P4-09 packages/workflow/workflow-worker-thread/src/index.ts": "HOT ZONE. From `git show d3a23a5e2c -- packages/workflow/workflow-worker-thread/src/index.ts`: the engine gains the definition registry, the per-run budget and ancestor map, `registerDefinition`, `startNested`, and two Config fields; `launch` gains its nested-limits argument. Shared with P4-07/P4-08 \u2014 the lease acquisition and journal directory in the same `launch` body are unchanged.",
    "P4-09 packages/workflow/workflow-worker-thread/src/protocol.ts": "The NestedStart / NestedSettled / NestedRefused messages. A nested run has to be started by the HOST, so the worker's request and the host's single reply need wire shapes; the file's existing tags are untouched.",
    "P4-09 packages/workflow/workflow-worker-thread/src/types.ts": "`ChildPort` gains `startNested` and `NestedStartRequest` is declared. The port is how a script's hook reaches the host, and the plan named the runtime and session files without naming the seam between them.",
    "P5-11 packages/run/taskboard-sqlite/src/index.ts": "The board's Cordis plugin. The durable store existed as `openTaskStore(directory)` only, so every consumer would have decided for its callers where tasks live -- and the directory IS the setting acceptance[0] turns on, since two hosts contend only when a profile pointed them at one board. Mirrors `@deepseek-ai/dsh-lease-sqlite` exactly, including opening the database in `Service.init`.",
    "P5-11 packages/subagent/subagent/src/control-types.ts": "Declares the optional `taskStatus` a listing row now carries. The declared consumer `list-children.ts` reads the board, and the type its rows are is here rather than in that file.",
    "P5-11 packages/subagent/subagent-taskboard/src/index.ts": "The producer the epic lacked. P5-11's plan named the primitives and the consumers but nothing that CREATES a task, which is why every clause subject had zero production callers (BLOCKED-154). Delegation is the moment the ruling names, and this plugin is the seam's Consumer role: it turns `subagent/start` and `subagent/end` into a claim and a receipt. It is a new package rather than an edit to `dsh-subagent` because the claim lease is a deployment-varying setting and `SubagentRuntime` has no Config to carry one.",
    "P5-11 packages/run/taskboard-sqlite/src/store.ts": "The durable board's implementation. It was `src/index.ts` until \u00a712.27-1 made that file the Cordis plugin, so the store moved down one file and the same code is now cited under the new name.",
    "P5-11 packages/collaboration/mailbox/src/index.ts": "Cited by the superseded P5-11.U.1 and by two live P4-06 entries, and the file no longer exists: `@deepseek-ai/dsh-mailbox` was retired into `@deepseek-ai/dsh-message-bus` under \u00a712.27-2. A freeze entry is append-only, so the citation stands as the record of what those entries observed; `FILES_REPLACED['P5-11']` carries the path forward for the registry.",
    "P4-07 packages/core/agent/src/types.ts": "Declares `Agent.leaseRefused`, the marker that made a refused lease distinguishable from a composition with no Run Service. The plan named the lease store and the fencing rule; the agent field is where the refusal has to be visible, because the dispatcher reads the agent rather than calling back into the Run Service (that would be a cycle).",
    "P4-07 packages/core/agent/src/dispatch.ts": "The single `advanceLeasedAgent` both the Run Service and the tool dispatcher reach, and where `lease-refused` is reported ahead of absence. The plan named no file for the entry point that presents the lease.",
    "P4-07 packages/core/agent-loop/src/tool-calls.ts": "Where the authority is actually presented: the dispatcher refuses a fenced or lease-refused run before a single tool runs, and writes the model-visible result that says which. A rule with no dispatch site refuses nothing, and the plan named no dispatch site.",
    "P4-07 packages/run/run/src/index.ts": "The Run Service is the holder (\u00a712.19-3): it takes the lease before registering the Run, renews it, releases it, and \u2014 since \u00a712.31-A \u2014 marks the agent when the store refuses. The plan named the lease library and not the service that holds a lease.",
    "P2-03 packages/core/tools/src/manifest-log.ts": "The session log as a real `ManifestAppender`. `appendManifestThenGate` is the one implementation of must[1]'s construct-append-gate order and the whole of must[2]'s no-bypass argument, and it needs a sink; the only one shipped was in-memory, so both dispatch paths wrote the order out for themselves and the shared implementation had no caller (\u00a712.33). It lives in `dsh-tools` rather than in `dsh-action-manifest` because a capability definition reaching for a session is the edge BLOCKED-136 records, and `dsh-tools` is already the runtime side both paths import."
  }
}
