# First-100 · 2026-09-11 日报(delegate first100-delegate-78)——终稿(2026-09-12 05:30 EDT)

## 一、结果
- **四笔推送到 `fork/first100-exec`**:① `e74593c50a`(凌晨);② `93d5220733`(18:44 EDT,候选 1′);③ `73c1c04f2e`(20:48 EDT,候选 2);④ **`6aa1e8273c`(05:24 EDT,候选 3′′′:P1-10/P4-01/P2-05 accept、P4-12 撤签、P1-07 (A′)、221/222/225 三条时序缺陷的产品修、四份 preFlight、3′′ 三笔修补)。远端现在 = `6aa1e8273c`,ACCEPTED 26 / BLOCKED_ON_ACCEPTANCE 3(P2-01、P5-10、P4-12)/ NOT_RUN 72。**
- **候选 3′ `ceee2ce233` 同一 SHA 两次云跑红在不同步,都是时序**:34666554965 红在 headless 回放一条场景(tool-jobs 通知投递竞态 → BLOCKED-222);34667961832 那步反而绿,却红在 P4-12 must[2] 双进程冻结用例(两个进程都拿到 reservation → BLOCKED-221,撤签 P4-12)。
- **候选 3′′ `2f3d3f4bab`**(= 3′ + lane A 屏障修 + lane B 221/225)07:03Z 派发 run 34679720296:**红 4 条,全是确定性的、全只在全套里活的检查**(新包中文 README 缺"概述"节;重刷 fixture 未投影成 canonical packed 布局;221 新增类型 `LedgerGeneration` 未入 cordis catalog ×2),其余 1216 文件 / 20996 用例过、他步全绿(含本机 OOM 的那道 frozen-titles 门,云上过了)。本机门集 23 PASS / 2 HELD / 1 OOM、lint 0、全语料 117/1/0 都没看见它们——因为本机全套跑不完。已立规则:新包/新类型/重刷 fixture 后报 tip 前必跑那三组门。**3′′′ `6ff9674a94`** = 三笔文档/布局/产物修补,08:00Z 派发 run 34682166773:**21000 用例全过、他步全绿,却因 1 条 unhandled rejection 退出 1**——run-store 的写在 dispose 期间逃出 disposer 的等待,测试删目录后 rename ENOENT(BLOCKED-229;两个文件候选里没改过,基线潜伏竞态)。**同一族红今晚三次(222 通知投递、221 reservation、229 dispose 逃逸),全是基线里的时序缺陷,候选只是照见它们;每一个都修了产品而不是重跑碰运气。** 229 修好后(`16897838c1`)我把同 SHA 重派了一次作为已知竞态的第二样本:34684115789 **全绿** → 第四笔推送落地;229/230 的修进候选 4。
- ACCEPTED:26 → **24**(撤签 P2-01 BLOCKED-200、P5-10 BLOCKED-215)→ 候选 3′′ 携 P1-10、P4-01(重签)、P2-05(新签)− P4-12(撤签)= **26**;READY 集 0 → 4(P2-06/P2-10/P2-12/P3-01)。
- 云跑:六次——125a4c8ec7 红(四因,全闭)、93d5220733 绿、73c1c04f2e 绿(含 hygiene 15/15)、ceee2ce233 ×2 红(两条时序缺陷各定因各修)、645ad6d6ca 进行中。
- 今日落地的产品/证据(摘要):P2-01.U2(host 身份 + 一跳委托)、P2-05 (a)/201 + 四项闭合、18 条词汇 backfill(make-vs-use 首次整门绿)、录制语料全刷(含 SDK 34 条事件恢复)、BLOCKED-197–226 的修与条目、hygiene 15 门进 exact-SHA workflow(210)、headless 发布包缺 chunk(207)、18+1 包进 SDK runtime 闭包(208)、coverage schema 首次被执行(206)、P4-01/P4-02/P4-11/P5-11 补冻结与覆盖、P1-07 (A′)(acp/web 上开)+ 持久授信记录(214 第二半,lane A 已建,语料一笔待查清)、P4-12 same-epoch 重取修(221)+ 崩溃演练真崩(225)、headless 回放竞态真屏障(222)、P3-01 C 阶段、P2-12 C + P 阶段(lane B,未冻结,候选 4)。

## 二、撤签与其原因(签核标准 4.4a–d 的执行)
- P2-01:出货 profile 不附着 host 身份(BLOCKED-200)→ U2 已修、语料已观测,待重签(重签材料含 tenant 写死的 OQ)。
- P5-10:acceptance[1](human answer 只送指定等待点)生产无接线(BLOCKED-215)→ 量出四处全缺(无生产调用者、无 `human-answer` 生产方、无宿主侧按 id 结算通道),**不是接线而是无主 seam**,改 DEFERRED-TO P2-12;P2-12 的 C/P 已把这四处以类型与用例封死,U 阶段做真实投递观测后 P5-10 才重签。
- **P4-12(今晚新增)**:must[2]"两个 worker 不能同时持有一个 reservation"只靠时序成立——prepared 同 epoch 重取返回 reserved,`markSent` 在事务外;我在 gate 树亲量 `reserved reserved`。根因:账本只记 epoch,无 lease 时 `?? 0` 让所有 agent 共用 epoch 0,并发 peer 与崩溃重启观测等价。修(221):有 lease 的 epoch 下同 epoch 重取拒绝(`held-at-same-epoch`)、更高 epoch 放行;无 lease 显式化为 unfenced、退化 at-least-once 并进审计;must[2] 改为条件性验收("fenced 下成立")。附带发现(225):acceptance[0] 的 10,000 次崩溃演练几乎不崩(LCG 双精度溢出,`random % 4` 几乎恒 0;修后四点各 ~2500 且 250 个 key 全覆盖)——它通过是因为不崩不是验到。两条都在候选 3′′ 里重观测,绿后重签。
- 不撤但补的:P6-01 [1] 用例证另一命题→补缺席主体 supplement;P0-06 [2] 空集真→限定落盘;P4-09 [2] 理由过期→改;P0-03 [1] 到期无执行者→BLOCKED-216(lane B 分支已修,代码进候选 4;3′′′ 只带了记录);P0-02 五处过期散文→BLOCKED-217(已修);P2-02 模块头过期→已修;P5-11 两进程用例在树里却未冻结→U.3。
- 正面样板:P1-03(划界)、P4-06(兑现自划之界)、P1-08(真 bin 观测)。P4-12 从"堵洞样板"改列为撤签——它的洞是自己堵的,但堵的位置在时序之外。

- **今晚追得最深的一条线(BLOCKED-229 → 230,已定因,在修)**:3′′′ 那条 unhandled rejection 追下去,是 run 服务两个 mount 的 disposer 从未运行,在飞的持久写(含 run 终态)无人等待。lane A 先怀疑 vendor cordis"根 Context 的 dispose 是 restart",被一条现成的绿冻结用例证伪后继续量,最终读数:**那条 spec 创建了四个 Context、只 dispose 了两个**——被丢弃的两个正是泄漏写的两个;disposer 不跑是必然,不是 cordis 缺陷。修分两半:测试侧 harness 统一在删目录前 dispose 并冻结通用断言(其余 22 个被冻结的同写法文件推送 4 后用守卫普查);产品侧从未 dispose 的 mount 其持久写失败必须有可观测 outcome(审计/事件),不许静默——生产里"进程退出前 Context 没走 dispose"同样会发生。vendor 未动。这条线上 lane A 三次自己推翻自己的结论,每次都有读数。

## 三、需要你定的(默认不动)
1. ~~P1-07 (A) 的另一半~~ 已裁「做」(01:46Z):持久授信记录 answerer,lane A 已建;语料刷新 92 文件分层查实后两条真阻塞(固定 seq 定位、`/private` 路径别名单向 → BLOCKED-223)在修,候选 4。
2. **已声明但无执行者的纪律**(到期日/owner/landsIn/义务转移/P0-06 registry 覆盖所有持久对象但生产注册者仅 settings 一个)在 4.4 标准下算不算缺口——推荐通则:新条目必须指出执行者或明写"无执行者"(已对新条目生效),旧条目不回溯。**默认按推荐。**
3. **unfenced 组合的外部副作用姿态**:出厂 profile 里没挂 Run Service 的 agent 无 lease,action-ledger 对它们只承诺 at-least-once。lane B 量出厂 bundle 哪些 profile 实为 unfenced 后进条目;要不要 fail-closed(无 lease 则拒绝外部副作用)是产品决定,推荐**先 fail-open + 审计标记**(今天的行为,显式化),等 P4-07 让每个 agent 都有 lease 后再收紧。
4. **子代理能否经父的 surface 向人提问(B1)**:今天 `dsh-user-questions` 对被拥有的子代理刻意拒绝(`DELEGATED_CALLER`,替代做法是"把未决问题写进子代理的最终结果"),所以 P5-10 acceptance[1]"人类答案只送到指定等待点"的主语在生产里不可达——不是漏接线,是设计如此。lane B 量了七段路径,缺三段、一段被禁。推荐:允许"子不直接问人、父代问"(改拒绝条件 + 造子→父转发与答案回送),跨 P2-06/P5-10/P2-12,冻结后作独立切片;**默认不做**,P5-10 维持撤签、BLOCKED-215 的收口悬于此决定。P2-12 的"ask question"动词按既有缝路径验收(web 真浏览器 e2e + SDK 首条 server→client 请求),控制面的等待点登记表只到"服务能做到"并明写限制。
5. `./src/*` 已裁(按推荐,BLOCKED-212,lane B 已做,候选 4)。文档预算余量与 `packages/README` 清单表豁免:非阻塞。

## 四、过程观察(进 ONBOARDING 的规则)
- 一个测过的观测 + 一句"显然为真"的从句;描述性文字不是现状;工具报"否"先确认它真的跑成了——今晚又添:snapshot 语料须 `--config vitest.snapshot.config.ts`(exit 1 无 summary = 零次执行,lane B 识别出来)、`vitest list --json <path>` exit 0 写 0 字节、截断 chunk 在"非空即成功"分支溜走、`cmd | tail; $?` 取的是 tail 的退出码(我自己一例)。
- 共享机器只杀自己的 PID(lane A `pkill -f vitest` 误杀 lane B 普查一次)。
- 竞态修法的约束:不许为测试确定性削弱生产及时性;真 happens-before 优于压小余量(222 用观察者插件而非文件门)。
- 用户令(22:23Z):关键路径优先,两笔推送落地前冻结债务发现——已执行;今晚新开的号(220–226)全部由关键路径上的红或撤签牵出,非主动发现。

## 五、机器
- 宿主颠簸(compressor 4.5 GB、free 60 MB、swapins 40 亿,外部 Chrome/Codex 占大头):全仓 `vitest list` 门本机 OOM ×5,由云上补;聚合回放跑不完,改按 lane 分跑。云跑为唯一全量观测。监视已收紧(v1 看门 + 一行心跳 + 10 分钟云跑轮询)。

## 六、你该知道的行为变化
- **headless/sdk 上的工作区信任边界默认开启**(候选 4):没有持久授信记录的项目,`AGENTS.md`/skills 不再自动加载(fail-closed);`--trust-workspace[=read|execute|none]` 或 `/trust-skills` 写入记录后放行,记录按工作区身份而非路径,grant/revoke 落审计;记录先于 approval policy 被咨询,所以 `policy: never` 的 unattended 部署有记录即放行。语料 92 文件中两个场景(`agent-instructions`、`ptc-workspace-context`)确实丢了项目指令内容(我先前"模型所见不变"的说法对这两个不成立),裁授信后重刷。
- **外部副作用账本(候选 3′′)**:有 lease 时同代重取被拒;无 lease 显式为 unfenced 并进审计;存储格式 v2,v1 文件 open 时拒绝。
- **headless 一次性 run 的已知丢失(BLOCKED-220,只记未修)**:run 到 idle 即收工,在飞后台 job 之后结算的结果模型永远看不到。
