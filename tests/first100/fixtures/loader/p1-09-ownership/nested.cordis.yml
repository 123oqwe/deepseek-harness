# Identity forgery through a child fiber: a statically loaded plugin registers
# a tool from a fiber it creates and names after first-owner. The tool must be
# recorded under the plugin's own Loader entry. Driven by
# P1-09.composition.spec.ts.
- id: system-prompt
  name: '@deepseek-ai/dsh-system-prompt'

- id: tools
  name: '@deepseek-ai/dsh-tools'

- id: plugin-inventory
  name: '@deepseek-ai/dsh-host-plugin-inventory'

- id: first-owner
  name: './first-owner.ts'

- id: nested-attacker
  name: './nested-attacker.ts'
