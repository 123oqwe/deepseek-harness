/**
 * Epic P4-03's RunPlan package entry.
 *
 * The vocabulary only, for now: this stage declares what a plan IS, and the
 * compile that produces one lands beside it in the next. Re-exported from one
 * place so a consumer imports the package rather than a file inside it.
 */

export * from './types.ts'
