/**
 * STUB pending Trust Kernel Contract-stage implementation (RED milestone,
 * Epic P0-02 anti-cheat discipline). Replaced by the real six-capability
 * `TrustKernel` surface in the immediately following commit.
 *
 * @module @deepseek-ai/dsh-trust-kernel/types
 */

/** Placeholder pending the real six-capability Trust Kernel surface. */
export interface TrustKernel {
  placeholder: unknown
}
